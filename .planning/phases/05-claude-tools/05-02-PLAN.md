---
phase: 05-claude-tools
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - pro-orc/components/toolsPanel.tsx
  - pro-orc/components/projectTabs.tsx
  - pro-orc/app/page.tsx
autonomous: true
requirements:
  - TOOL-04

must_haves:
  truths:
    - "Dashboard has a third tab labeled 'Tools' alongside Code and Research"
    - "Tools tab displays skills with name, type badge, and description"
    - "Tools tab displays MCP-backed plugins with name, type badge, and description"
    - "Tools tab displays skill-only plugins with name, type badge, and description"
    - "Tools tab shows a count badge on the tab trigger matching total tool count"
  artifacts:
    - path: "pro-orc/components/toolsPanel.tsx"
      provides: "ToolsPanel component rendering categorized tool cards"
      min_lines: 40
    - path: "pro-orc/components/projectTabs.tsx"
      provides: "Updated ProjectTabs with Tools tab"
      contains: "tools"
    - path: "pro-orc/app/page.tsx"
      provides: "scanClaudeTools() call and tools prop passing"
      contains: "scanClaudeTools"
  key_links:
    - from: "pro-orc/app/page.tsx"
      to: "pro-orc/lib/tools-scanner.ts"
      via: "import and call scanClaudeTools()"
      pattern: "scanClaudeTools"
    - from: "pro-orc/app/page.tsx"
      to: "pro-orc/components/projectTabs.tsx"
      via: "tools prop"
      pattern: "tools="
    - from: "pro-orc/components/projectTabs.tsx"
      to: "pro-orc/components/toolsPanel.tsx"
      via: "ToolsPanel component render in TabsContent"
      pattern: "ToolsPanel"
---

<objective>
Create the Tools panel component and wire it into the dashboard as a third tab, displaying all auto-discovered Claude capabilities grouped by category (Skills, MCP Servers, Plugins).

Purpose: Completes Phase 5 by making the scanned tool data visible in the UI.
Output: `components/toolsPanel.tsx`, updated `components/projectTabs.tsx` with Tools tab, updated `app/page.tsx` with `scanClaudeTools()` call.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-claude-tools/05-RESEARCH.md
@.planning/phases/05-claude-tools/05-01-SUMMARY.md

@pro-orc/lib/types.ts
@pro-orc/components/projectTabs.tsx
@pro-orc/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToolsPanel component</name>
  <files>pro-orc/components/toolsPanel.tsx</files>
  <action>
Create `pro-orc/components/toolsPanel.tsx` as a client component (`'use client'`).

**Props:** `{ tools: ClaudeToolsData }` — import `ClaudeToolsData, ClaudeTool` from `@/lib/types`.

**Layout:** Three sections rendered vertically, each with a category heading and a grid of tool cards. Only render a section if its array is non-empty.

**Sections:**
1. **Skills** (`tools.skills`) — heading "Skills" with a `Sparkles` icon from lucide-react
2. **MCP Servers** (`tools.mcpPlugins`) — heading "MCP Servers" with a `Server` icon from lucide-react
3. **Plugins** (`tools.skillPlugins`) — heading "Plugins" with a `Puzzle` icon from lucide-react

**Each tool card:** Use shadcn `Card` (already installed). Display:
- Tool `name` as card title (font-medium)
- `type` as a small Badge (use shadcn Badge if available, or a styled span): "skill" = cyan variant, "mcp" = fuchsia variant, "plugin" = neutral variant
- `description` as muted text below the name (truncate to 2 lines with `line-clamp-2`)
- For plugins: if `enabled === false`, show a subtle "Disabled" indicator (e.g., dimmed opacity or a small badge)
- For plugins with `marketplace`: show marketplace name in mono text-xs

**Styling:** Follow existing glassmorphism card patterns — `bg-card/40 backdrop-blur-sm border-border`. Use the same grid pattern: `grid grid-cols-1 gap-3 md:grid-cols-2 lg:grid-cols-3`.

**Empty state:** If `tools.skills.length + tools.mcpPlugins.length + tools.skillPlugins.length === 0`, show a centered message: "No Claude tools found in ~/.claude/"

**Category heading style:** Use `flex items-center gap-2 text-sm font-medium text-muted-foreground mb-3` with a lucide icon at `size-4`.
  </action>
  <verify>Run `npx tsc --noEmit` from `pro-orc/` — no type errors.</verify>
  <done>ToolsPanel component exists, renders three tool categories with cards, compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Tools tab into ProjectTabs and page.tsx</name>
  <files>pro-orc/components/projectTabs.tsx, pro-orc/app/page.tsx</files>
  <action>
**In `app/page.tsx`:**
1. Add import: `import { scanClaudeTools } from '@/lib/tools-scanner'`
2. Add import for `ClaudeToolsData` from types (if needed for typing)
3. Change `scanProjects()` call to run concurrently with `scanClaudeTools()`:
   ```typescript
   const [projects, tools] = await Promise.all([
     scanProjects(),
     scanClaudeTools(),
   ])
   ```
4. Pass `tools` prop to `ProjectTabs`:
   ```tsx
   <ProjectTabs
     codeProjects={codeProjects}
     researchProjects={researchProjects}
     tools={tools}
   />
   ```

**In `components/projectTabs.tsx`:**
1. Add import for `ToolsPanel` from `@/components/toolsPanel`
2. Add import for `ClaudeToolsData` from `@/lib/types`
3. Add import for `Wrench` from `lucide-react`
4. Update the component props type to accept `tools: ClaudeToolsData`
5. Calculate `totalToolCount = tools.skills.length + tools.mcpPlugins.length + tools.skillPlugins.length`
6. Add a third `TabsTrigger` for "tools" value AFTER the Research trigger, using the `Wrench` icon:
   ```tsx
   <TabsTrigger
     value="tools"
     className={cn(
       triggerBase,
       'data-[state=active]:border-border data-[state=active]:bg-card/60',
       'data-[state=active]:text-foreground data-[state=active]:shadow-[0_0_12px_oklch(0.715_0.143_212.34/0.05)]',
     )}
   >
     <Wrench className="size-4" />
     Tools
     <span className="rounded-full bg-muted px-2 py-0.5 font-mono text-xs text-muted-foreground">
       {totalToolCount}
     </span>
   </TabsTrigger>
   ```
7. Add a third `TabsContent` for "tools" value at the end, inside the bordered container div:
   ```tsx
   <TabsContent value="tools" className="mt-0">
     <ToolsPanel tools={tools} />
   </TabsContent>
   ```

**Important:** The Tools tab styling should use a neutral color (not cyan or fuchsia) since it's a utility panel, not a project type. Use `text-foreground` for active state and `bg-muted` for the count badge.
  </action>
  <verify>Run `npx tsc --noEmit` from `pro-orc/`. Then run `npm run dev` and visit localhost:3000 — three tabs should be visible: Code, Research, Tools.</verify>
  <done>Dashboard shows three tabs. Clicking "Tools" displays skills, MCP servers, and plugins discovered from ~/.claude/. Count badge on tab shows total tool count.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes from `pro-orc/`
2. `npm run dev` starts without errors
3. localhost:3000 shows three tabs: Code, Research, Tools
4. Tools tab shows tool cards grouped by Skills, MCP Servers, Plugins
5. Each card shows name, type badge, and description
6. Tool count badge on tab matches actual number of discovered tools
</verification>

<success_criteria>
- Tools tab is visible alongside Code and Research tabs
- Skills section shows entries from ~/.claude/skills/
- MCP Servers section shows plugins with .mcp.json
- Plugins section shows plugins without .mcp.json
- All tools show name, type, and description
- No manual configuration required — data loads automatically at startup
</success_criteria>

<output>
After completion, create `.planning/phases/05-claude-tools/05-02-SUMMARY.md`
</output>
