---
phase: 18-external-resource-cleanup
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - pro_orc/lib/features/shared/delete_project_dialog.dart
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03, CLN-04, CLN-05]

must_haves:
  truths:
    - "Dialog shows detected external resources as a scrollable list between the warning box and the name input field"
    - "Each resource row has a checkbox (default unchecked) with icon, label, and URI"
    - "Resources marked for cleanup show their hint text explaining how to delete them"
    - "After local project deletion, dialog shows a summary of marked resources with actionable hints — does NOT auto-delete them"
    - "If no external resources are detected, the dialog looks identical to the Phase 17 version (no empty state clutter)"
  artifacts:
    - path: "pro_orc/lib/features/shared/delete_project_dialog.dart"
      provides: "Extended DeleteProjectDialog with resource detection and step-by-step display"
      contains: "detectExternalResources"
  key_links:
    - from: "pro_orc/lib/features/shared/delete_project_dialog.dart"
      to: "pro_orc/lib/data/services/resource_detector.dart"
      via: "calls detectExternalResources in initState"
      pattern: "detectExternalResources"
    - from: "pro_orc/lib/features/shared/delete_project_dialog.dart"
      to: "pro_orc/lib/data/models/external_resource.dart"
      via: "imports ExternalResource and ExternalResourceType for rendering"
      pattern: "ExternalResource"
---

<objective>
Extend the DeleteProjectDialog to detect and display external resources with per-resource checkboxes, and show a post-deletion summary of marked resources with actionable cleanup hints.

Purpose: Users need to see what external resources are linked before deleting, and get guidance on cleaning them up after local deletion.
Output: Enhanced DeleteProjectDialog with resource detection, checkboxes, and post-deletion summary.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-external-resource-cleanup/18-01-SUMMARY.md
@pro_orc/lib/features/shared/delete_project_dialog.dart
@pro_orc/lib/data/models/external_resource.dart
@pro_orc/lib/data/services/resource_detector.dart
@pro_orc/lib/data/models/project_model.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resource detection and checkbox UI to DeleteProjectDialog</name>
  <files>pro_orc/lib/features/shared/delete_project_dialog.dart</files>
  <action>
Extend the existing DeleteProjectDialog with resource detection. The dialog already works — add resource display between the warning box and the project name section.

**New state fields:**
- `List<ExternalResource>? _resources` — null = loading, empty = none found
- `Set<int> _selectedResources` — indices of resources user wants to clean up (all unchecked by default)
- `bool _showSummary` — false initially, true after deletion completes (to show cleanup summary)

**In initState:**
After existing `_textController` setup, call `_loadResources()`:
```dart
void _loadResources() async {
  final resources = await detectExternalResources(widget.project);
  if (mounted) setState(() => _resources = resources);
}
```

**Resource list UI (new `_buildResources` method):**
Insert between `_buildWarning` and `_buildProjectName` in the Column children — but ONLY if `_resources != null && _resources!.isNotEmpty`.

Display as a Container with same style as the warning box but with `colors.bgElev.withValues(alpha: 0.3)` background and `colors.textDim.withValues(alpha: 0.2)` border (not red — informational, not destructive).

Header text: "Verknuepfte externe Ressourcen" in `colors.textSec`, fontSize 12, fontWeight w600.

Each resource as a Row with:
- Checkbox (small, uses `colors.textDim` when unchecked, `Colors.amber.shade600` when checked)
- Icon based on type: `Icons.article_outlined` for notion, `Icons.code` for github, `Icons.palette_outlined` for figma, `Icons.psychology_outlined` for claudeMemory, `Icons.link` for other
- Column with label (textPri, fontSize 13) and uri truncated to 50 chars (textDim, fontSize 11)

If `_resources` is null (still loading), show nothing (don't block the dialog — resources are supplementary info).

Wrap the resource list in a ConstrainedBox(maxHeight: 180) with a SingleChildScrollView for overflow.

**Modified _onDelete:**
After successful deletion (`success == true`), check if any resources were selected (`_selectedResources.isNotEmpty`). If yes, set `_showSummary = true` via setState instead of immediately popping. If no resources selected, pop as before.

**Post-deletion summary UI (new `_buildSummary` method):**
When `_showSummary == true`, replace the entire dialog body with:
- Header: "Projekt geloescht" with green checkmark icon
- Subheader: "Folgende Ressourcen muessen manuell bereinigt werden:"
- For each selected resource: icon + label + full URI + hint text (the hint from ExternalResource)
- Single "Schliessen" button that pops with true

The summary uses the same glassmorphism container. Each resource in the summary should show the full URI (not truncated) and the hint text in `colors.textDim` fontSize 12.

**Important constraints:**
- Do NOT change the existing name-confirmation logic or the warning box
- Do NOT auto-delete any external resources — this is informational only
- If zero resources detected, the dialog is unchanged from Phase 17
- Keep the existing BackdropFilter/glassmorphism styling
- Use `withValues(alpha:)` not deprecated `withOpacity()`
  </action>
  <verify>
Run `cd pro_orc && flutter analyze` — zero issues.
Verify the dialog still compiles and the new imports for ExternalResource and detectExternalResources are present.
  </verify>
  <done>
DeleteProjectDialog detects external resources on open, displays them with per-resource checkboxes, and after deletion shows a summary of marked resources with cleanup hints. If no resources found, dialog is unchanged from Phase 17.
  </done>
</task>

</tasks>

<verification>
1. `cd pro_orc && flutter analyze` — zero issues
2. Dialog imports resource_detector and external_resource
3. Resource detection runs in initState (non-blocking)
4. Each resource type has an appropriate icon
5. Checkboxes are unchecked by default (CLN-05: no resource deleted without explicit confirmation)
6. Post-deletion summary shows hint text for each marked resource
7. Zero-resource case: dialog identical to Phase 17 version
</verification>

<success_criteria>
- Dialog compiles and analyzes clean
- Resource list appears between warning and name input (when resources exist)
- Each resource is individually selectable via checkbox (CLN-05)
- Post-deletion summary shows cleanup guidance for selected resources
- No external resources are auto-deleted — informational only
- Notion (CLN-01), GitHub (CLN-02), Figma (CLN-03), Claude Memory (CLN-04) all recognized
</success_criteria>

<output>
After completion, create `.planning/phases/18-external-resource-cleanup/18-02-SUMMARY.md`
</output>
