---
phase: 14-add-card-dialog
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - pro_orc/lib/features/shared/create_project_dialog.dart
  - pro_orc/lib/features/code/code_tab.dart
  - pro_orc/lib/features/research/research_tab.dart
autonomous: false
requirements: [DLG-01, DLG-02, DLG-03, DLG-04, DLG-05, DLG-06, DLG-07, DLG-08]

must_haves:
  truths:
    - "Dialog oeffnet sich mit vorausgewaehltem Tab (Code oder Research) basierend auf Herkunfts-Tab"
    - "Tab-Switcher wechselt zwischen Code- und Research-Optionen ohne Dialog zu schliessen"
    - "Namensfeld zeigt Echtzeit-Ordnername-Vorschau und validiert gegen existierende Ordner"
    - "Code-Tab zeigt git init Toggle (ON), GSD Skeleton Toggle (ON), rem-sleep Toggle (OFF), Zielordner Dropdown"
    - "Research-Tab zeigt Notion-Seite Toggle (ON), rem-sleep Toggle (ON), Zielordner Dropdown"
    - "Erstellen-Button ist disabled bei leerem/ungueltigem Namen"
    - "Dialog schliesst via Overlay-Klick, X-Button oder Abbrechen"
  artifacts:
    - path: "pro_orc/lib/features/shared/create_project_dialog.dart"
      provides: "CreateProjectDialog with TabBar, form fields, toggles, folder dropdown"
      min_lines: 200
    - path: "pro_orc/lib/features/code/code_tab.dart"
      provides: "Wired _openCreateDialog method showing CreateProjectDialog"
      contains: "CreateProjectDialog"
    - path: "pro_orc/lib/features/research/research_tab.dart"
      provides: "Wired _openCreateDialog method showing CreateProjectDialog"
      contains: "CreateProjectDialog"
  key_links:
    - from: "pro_orc/lib/features/code/code_tab.dart"
      to: "pro_orc/lib/features/shared/create_project_dialog.dart"
      via: "_openCreateDialog calls showDialog with CreateProjectDialog"
      pattern: "showDialog.*CreateProjectDialog"
    - from: "pro_orc/lib/features/shared/create_project_dialog.dart"
      to: "pro_orc/lib/providers/database_provider.dart"
      via: "Reads scan dirs from DB for Zielordner dropdown"
      pattern: "getScanDirs"
---

<objective>
CreateProjectDialog Widget mit Tab-Switcher (Code/Research), Namensfeld mit Live-Ordnername-Vorschau, typ-spezifischen Toggles und Zielordner-Dropdown. Wired in Code- und Research-Tab.

Purpose: User kann alle Optionen fuer das neue Projekt konfigurieren bevor die Erstellung (Phase 15) gestartet wird. Nur UI — kein Filesystem-Zugriff ausser Ordner-Existenz-Pruefung.
Output: `create_project_dialog.dart` Widget + Wiring in beiden Tabs

Note: DLG-07 (CLAUDE.md Toggle) und DLG-08 (.gitignore Dropdown) sind per User-Entscheidung aus Phase 14 entfernt (deferred). Die Requirement-IDs werden hier gefuehrt aber nicht implementiert.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-add-card-dialog/14-01-SUMMARY.md
@pro_orc/lib/features/shell/glass_card.dart
@pro_orc/lib/features/code/code_tab.dart
@pro_orc/lib/features/research/research_tab.dart
@pro_orc/lib/features/settings/settings_tab.dart
@pro_orc/lib/providers/database_provider.dart
@pro_orc/lib/data/db/app_database.dart
@pro_orc/lib/theme/n3_colors.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: CreateProjectDialog Widget</name>
  <files>pro_orc/lib/features/shared/create_project_dialog.dart</files>
  <action>
Create `CreateProjectDialog` as a `ConsumerStatefulWidget` (needs ref for DB access). It must also use `SingleTickerProviderStateMixin` for TabController.

**Parameters:**
- `String initialTab` — `'code'` or `'research'`, sets initial TabController index

**Dialog structure (locked decisions):**
- Show via `showDialog` with `barrierDismissible: true` (closes on overlay click)
- Dialog width: 480px, constrained via `ConstrainedBox` or `SizedBox`
- Use glassmorphism style matching n3urala1 theme: Container with `bgSurf` background, rounded corners (14), no Material Dialog chrome. Use `Dialog(backgroundColor: Colors.transparent, child: ...)` wrapper and build the glass container manually with ClipRRect + BackdropFilter pattern from GlassCard.
- Padding: 24px all around

**Header (locked decisions):**
- Title text: dynamically switches between "Neues Code-Projekt" and "Neues Research-Projekt" based on active tab
- X-Button (Icons.close) top-right corner, closes dialog via `Navigator.of(context).pop()`
- Title style: textPri, fontSize 18, fontWeight w600

**TabBar (locked decisions):**
- Two tabs: "Code" and "Research"
- Use Flutter `TabBar` with `TabController` (length: 2, initialIndex from parameter)
- Tab indicator: underline style, color dynamically changes — cyan when Code tab active, fuchsia when Research tab active
- Label color: textPri, unselected: textDim
- Tab text style: fontSize 14, fontWeight w500
- Listen to TabController changes to rebuild form fields and update accent color

**Form fields — shared between tabs:**
- `TextField` for project name (DLG-01):
  - Autofocus: true (locked decision)
  - Hint: "Projektname"
  - Style: textPri, fontSize 14, filled with bgElev.withValues(alpha: 0.4)
  - `onChanged`: update state, derive folder name, check existence
  - Enter key does NOT submit (do NOT add onSubmitted handler)
- Folder name preview below TextField:
  - Text: "Ordner: {derived_name}" in textSec, fontSize 12
  - Derive: trim, lowercase, replace spaces with `_`, remove chars not matching `[a-z0-9_-]`
  - If derived name is empty, show nothing
  - If folder already exists in selected scan dir, show error text in amber: "Ordner existiert bereits"
  - Check existence via `Directory(path.join(selectedScanDir, derivedName)).existsSync()`
- Zielordner Dropdown:
  - Load scan dirs from DB via `ref.read(appDatabaseProvider).getScanDirs()` in initState
  - `DropdownButtonFormField<String>` showing abbreviated paths (replace `/Users/xxx` with `~`)
  - Default: first scan dir in list
  - Style consistent with settings tab: bgElev fill, textPri text, no border

**Form fields — Code tab (when TabController.index == 0):**
- Toggle: "Git Repository initialisieren" — default ON (DLG-02)
- Toggle: "GSD Skeleton anlegen" — default ON (DLG-03, note: context changed default from ROADMAP's OFF to ON)
- Toggle: "rem-sleep nach Erstellung" — default OFF (DLG-05)

**Form fields — Research tab (when TabController.index == 1):**
- Toggle: "Notion-Seite erstellen" — default ON (DLG-04)
- Toggle: "rem-sleep nach Erstellung" — default ON (DLG-05, note: research default is ON per context)

**Toggle style (Claude's discretion):**
- Use `SwitchListTile.adaptive` for each toggle
- Active color: current tab accent (cyan/fuchsia)
- Title: textPri, fontSize 13
- Dense: true, contentPadding horizontal only
- Wrap toggles in AnimatedSwitcher or AnimatedCrossFade for tab-switch transition (fade, 200ms)

**Bottom buttons (locked decisions):**
- Row with MainAxisAlignment.end
- "Abbrechen" TextButton — closes dialog
- "Erstellen" FilledButton in accent color (cyan/fuchsia based on active tab)
- Erstellen disabled when: derived folder name is empty OR folder already exists
- Erstellen onPressed: for now, just pop the dialog and return a result Map with all form values. Phase 15 wires the actual creation logic.
- Return value structure: `{'name': displayName, 'folderName': derivedName, 'scanDir': selectedDir, 'tab': 'code'|'research', 'gitInit': bool, 'gsdSkeleton': bool, 'notion': bool, 'remSleep': bool}`

**State management:**
- `_nameController` TextEditingController
- `_derivedFolderName` String
- `_folderExists` bool
- `_selectedScanDir` String?
- `_scanDirs` List<String>
- `_gitInit`, `_gsdSkeleton`, `_notion`, `_remSleep` bools with defaults per tab
- When tab switches, reset toggles to that tab's defaults
- `_isLoading` bool for future create button spinner (Phase 15)

**Important — Deferred items (DO NOT implement):**
- DLG-07: CLAUDE.md toggle — removed per user decision
- DLG-08: .gitignore dropdown — removed per user decision
- DLG-06: Terminal toggle — removed per user decision (rem-sleep implies terminal)
  </action>
  <verify>
`flutter analyze` reports no issues. Widget compiles and accepts initialTab parameter. All toggles have correct defaults per tab.
  </verify>
  <done>CreateProjectDialog renders with TabBar, name field with live folder preview, tab-specific toggles with correct defaults, Zielordner dropdown, and disabled Erstellen button when name invalid. Returns form values as Map on submit.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Dialog into Code and Research tabs</name>
  <files>pro_orc/lib/features/code/code_tab.dart, pro_orc/lib/features/research/research_tab.dart</files>
  <action>
**Code Tab (`code_tab.dart`):**

Import `create_project_dialog.dart`. Replace the placeholder `_openCreateDialog` method (from Plan 14-01) with:

```dart
void _openCreateDialog(BuildContext context, String initialTab) {
  showDialog<Map<String, dynamic>>(
    context: context,
    barrierDismissible: true,
    barrierColor: Colors.black54,
    builder: (context) => CreateProjectDialog(initialTab: initialTab),
  );
  // Result handling wired in Phase 15
}
```

**Research Tab (`research_tab.dart`):**

Same import and same `_openCreateDialog` method implementation. The AddProjectCard from Plan 14-01 already passes the correct initialTab string.

Ensure both tabs import the dialog widget with package import.
  </action>
  <verify>
`flutter analyze` passes. Run `flutter run -d macos` and verify:
1. Click Add+ card in Code tab — dialog opens with Code tab pre-selected, cyan accent
2. Click Add+ card in Research tab — dialog opens with Research tab pre-selected, fuchsia accent
3. Tab switcher in dialog works without closing dialog
4. Name field has autofocus, shows folder preview on typing
5. Toggles switch between Code/Research options when tab changes
6. Erstellen button disabled when name empty, enabled when valid name entered
7. Dialog closes on: overlay click, X button, Abbrechen button
  </verify>
  <done>Klick auf Add+ Karte oeffnet CreateProjectDialog mit korrektem vorausgewaehltem Tab. Dialog ist voll funktional (UI only, keine Erstellung).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual Verification of Add Card + Dialog</name>
  <files>pro_orc/lib/features/shared/add_project_card.dart, pro_orc/lib/features/shared/create_project_dialog.dart</files>
  <action>
Verify Add+ Karte in beiden Grids und CreateProjectDialog mit Tab-Switcher, Namensfeld-Validierung, Toggles und Zielordner-Dropdown.

`flutter run -d macos` starten und folgende Punkte pruefen:
1. Code-Tab: Add+ Karte am Ende des Grids sichtbar (cyan, ghost-style)?
2. Research-Tab: Add+ Karte am Ende des Grids sichtbar (fuchsia, ghost-style)?
3. Hover ueber Add+ Karte: Opazitaet steigt, leichter Scale, Glow-Effekt?
4. Klick auf Add+ im Code-Tab: Dialog oeffnet mit "Neues Code-Projekt" Titel, Code-Tab aktiv?
5. Klick auf Add+ im Research-Tab: Dialog oeffnet mit "Neues Research-Projekt", Research-Tab aktiv?
6. Tab-Switcher im Dialog: Wechsel zwischen Code/Research aendert Toggles und Akzentfarbe?
7. Namensfeld: Tippe "My Cool Project" — Vorschau zeigt "Ordner: my_cool_project"?
8. Toggles Code: git init (ON), GSD Skeleton (ON), rem-sleep (OFF)?
9. Toggles Research: Notion (ON), rem-sleep (ON)?
10. Erstellen-Button disabled bei leerem Namen, enabled bei gueltigem Namen?
11. Dialog schliesst via X, Abbrechen, und Klick ausserhalb?
  </action>
  <verify>Human visual verification of all 11 points above.</verify>
  <done>All visual and interaction behaviors confirmed working by user.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — zero issues
2. Dialog opens from both tabs with correct initial tab selection
3. Tab switcher animates underline color (cyan ↔ fuchsia)
4. Name validation: live preview, existing folder detection, empty name blocks submit
5. Toggles: correct defaults per tab, switch on tab change
6. Zielordner dropdown: lists all configured scan dirs from DB
7. Dialog dismissal: all three methods work (overlay, X, Abbrechen)
8. No regression: project cards, hidden banner, grid layout unchanged
</verification>

<success_criteria>
- CreateProjectDialog exists with TabBar (Code/Research), name field, toggles, Zielordner dropdown
- Dialog opens from Code tab Add+ with Code pre-selected (cyan)
- Dialog opens from Research tab Add+ with Research pre-selected (fuchsia)
- Name field validates in real-time with folder name preview
- Erstellen returns form values as Map (ready for Phase 15 wiring)
- `flutter analyze` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/14-add-card-dialog/14-02-SUMMARY.md`
</output>
