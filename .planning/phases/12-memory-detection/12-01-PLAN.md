---
phase: 12-memory-detection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pro_orc/lib/data/models/memory_data.dart
  - pro_orc/lib/data/services/memory_reader.dart
  - pro_orc/test/data/memory_reader_test.dart
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03

must_haves:
  truths:
    - "encodeProjectPath converts absolute paths to Claude-style dash-separated format"
    - "MemoryReader detects MEMORY.md existence at the correct encoded path"
    - "MemoryReader reads mtime of MEMORY.md and computes stale status (>7 days)"
    - "Missing memory directory returns MemoryData with hasMemory=false"
  artifacts:
    - path: "pro_orc/lib/data/models/memory_data.dart"
      provides: "MemoryData model with hasMemory, lastConsolidated, isStale"
      contains: "class MemoryData"
    - path: "pro_orc/lib/data/services/memory_reader.dart"
      provides: "encodeProjectPath function and readMemoryData function"
      exports: ["encodeProjectPath", "readMemoryData"]
    - path: "pro_orc/test/data/memory_reader_test.dart"
      provides: "Unit tests for path encoding, detection, mtime, missing file"
      min_lines: 40
  key_links:
    - from: "pro_orc/lib/data/services/memory_reader.dart"
      to: "pro_orc/lib/data/models/memory_data.dart"
      via: "returns MemoryData from readMemoryData"
      pattern: "MemoryData"
---

<objective>
TDD-develop the MemoryData model and MemoryReader service that detects Claude rem-sleep memory consolidation status for a given project path.

Purpose: Foundation for memory indicator — pure Dart service that encodes project paths, checks filesystem for MEMORY.md, and returns structured status data.
Output: MemoryData model, MemoryReader service (two top-level functions), comprehensive unit tests.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pro_orc/lib/data/models/git_data.dart
@pro_orc/lib/data/services/git_reader.dart
@pro_orc/test/data/git_reader_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — MemoryData model + MemoryReader tests</name>
  <files>
    pro_orc/lib/data/models/memory_data.dart
    pro_orc/lib/data/services/memory_reader.dart
    pro_orc/test/data/memory_reader_test.dart
  </files>
  <action>
Create MemoryData model in `memory_data.dart` following GitData pattern:
- `hasMemory` (bool) — whether MEMORY.md exists
- `lastConsolidated` (DateTime?) — mtime of MEMORY.md, null if not found
- `isStale` (bool) — true if lastConsolidated is older than 7 days
- `static const empty = MemoryData()` with hasMemory=false, isStale=false
- Constructor with named parameters, all optional except hasMemory which defaults to false

Create minimal stubs in `memory_reader.dart` (just function signatures that throw UnimplementedError):
- `String encodeProjectPath(String projectPath)` — top-level function
- `Future<MemoryData> readMemoryData(String projectPath, {String? claudeHomeDirOverride})` — top-level function. The override parameter allows tests to use a temp dir instead of `~/.claude`.

Create `memory_reader_test.dart` with tests using real temp directories (follow git_reader_test.dart pattern with `Directory.systemTemp.createTemp` and `addTearDown`):

Group 1 — `encodeProjectPath`:
- `/Users/rob/code/foo` returns `-Users-rob-code-foo`
- `/` returns `-`
- `/Users/rob/code/my project` (with space) returns `-Users-rob-code-my project` (spaces preserved, only slashes become dashes)

Group 2 — `readMemoryData` (use claudeHomeDirOverride pointing to temp dir):
- Project with existing MEMORY.md: create temp dir structure `{claudeHome}/projects/{encodedPath}/memory/MEMORY.md`, write content, call readMemoryData. Expect hasMemory=true, lastConsolidated != null, isStale=false (file just created).
- Project with no memory dir at all: expect hasMemory=false, lastConsolidated=null, isStale=false.
- Project with memory dir but no MEMORY.md (Claude created project dir but rem-sleep never ran): create `{claudeHome}/projects/{encodedPath}/` but no memory/ subfolder. Expect hasMemory=false.

Run `flutter test test/data/memory_reader_test.dart` — tests MUST FAIL (stubs throw UnimplementedError).
Commit: `test(12-01): add failing tests for MemoryReader`
  </action>
  <verify>`flutter test test/data/memory_reader_test.dart` shows failures (not compilation errors)</verify>
  <done>Test file exists with 5+ test cases, all fail due to unimplemented stubs</done>
</task>

<task type="auto">
  <name>Task 2: GREEN + REFACTOR — Implement MemoryReader</name>
  <files>
    pro_orc/lib/data/services/memory_reader.dart
  </files>
  <action>
Implement both functions in `memory_reader.dart`:

`encodeProjectPath(String projectPath)`:
- Replace every `/` with `-`. That is the entire encoding. `/Users/rob/code/foo` becomes `-Users-rob-code-foo`.
- Use `projectPath.replaceAll('/', '-')`.

`readMemoryData(String projectPath, {String? claudeHomeDirOverride})`:
- Determine Claude home: `claudeHomeDirOverride ?? p.join(Platform.environment['HOME']!, '.claude')`
- Encode the project path via `encodeProjectPath(projectPath)`
- Build memory file path: `p.join(claudeHome, 'projects', encodedPath, 'memory', 'MEMORY.md')`
- Use `File(memoryPath).existsSync()` to check existence (sync is fine — called per project, not hot path)
- If exists: read `FileStat.statSync(memoryPath).modified` for mtime, compute `isStale = DateTime.now().difference(mtime) > Duration(days: 7)`
- If not exists: return `MemoryData.empty`
- Wrap in try/catch, return `MemoryData.empty` on any error (permission denied, etc.)

Import `dart:io` and `package:path/path.dart as p`. Use package imports only.

Run `flutter test test/data/memory_reader_test.dart` — all tests MUST PASS.
Commit: `feat(12-01): implement MemoryReader with path encoding`

If any cleanup needed (naming, formatting), refactor and commit: `refactor(12-01): clean up MemoryReader`
  </action>
  <verify>`flutter test test/data/memory_reader_test.dart` — all tests pass. `flutter analyze` — no new issues.</verify>
  <done>All memory_reader tests green, no analyzer warnings in new files</done>
</task>

</tasks>

<verification>
- `flutter test test/data/memory_reader_test.dart` — all pass
- `flutter analyze` — clean on new files
- MemoryData model has hasMemory, lastConsolidated, isStale fields
- encodeProjectPath correctly handles slash-to-dash conversion
- readMemoryData returns correct MemoryData for existing/missing MEMORY.md
</verification>

<success_criteria>
- MemoryData model exists with documented fields and static empty constant
- encodeProjectPath converts paths correctly (verified by 3+ test cases)
- readMemoryData detects presence/absence of MEMORY.md (verified by 3+ test cases)
- readMemoryData reads mtime and computes stale flag
- All tests pass, no analyzer issues
</success_criteria>

<output>
After completion, create `.planning/phases/12-memory-detection/12-01-SUMMARY.md`
</output>
