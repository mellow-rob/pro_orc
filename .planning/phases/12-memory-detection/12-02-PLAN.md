---
phase: 12-memory-detection
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - pro_orc/lib/data/models/project_model.dart
  - pro_orc/lib/data/services/project_scanner.dart
  - pro_orc/test/data/project_scanner_test.dart
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03

must_haves:
  truths:
    - "ProjectModel carries memory status for each scanned project"
    - "ProjectScanner calls MemoryReader during scanAll and populates ProjectModel.memory"
    - "Existing ProjectScanner tests still pass after integration"
  artifacts:
    - path: "pro_orc/lib/data/models/project_model.dart"
      provides: "ProjectModel with optional MemoryData field"
      contains: "MemoryData"
    - path: "pro_orc/lib/data/services/project_scanner.dart"
      provides: "scanAll calls readMemoryData per project"
      contains: "readMemoryData"
    - path: "pro_orc/test/data/project_scanner_test.dart"
      provides: "Test verifying memory field populated on scanned projects"
      contains: "memory"
  key_links:
    - from: "pro_orc/lib/data/services/project_scanner.dart"
      to: "pro_orc/lib/data/services/memory_reader.dart"
      via: "import and call readMemoryData in scanAll loop"
      pattern: "readMemoryData"
    - from: "pro_orc/lib/data/models/project_model.dart"
      to: "pro_orc/lib/data/models/memory_data.dart"
      via: "import MemoryData type for memory field"
      pattern: "MemoryData"
---

<objective>
Wire MemoryReader into the ProjectScanner pipeline so every ProjectModel includes memory consolidation status.

Purpose: Complete the data layer integration so Phase 13 (UI) can read memory status directly from ProjectModel.
Output: Updated ProjectModel with memory field, ProjectScanner calling MemoryReader, integration test.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-memory-detection/12-01-SUMMARY.md
@pro_orc/lib/data/models/project_model.dart
@pro_orc/lib/data/services/project_scanner.dart
@pro_orc/test/data/project_scanner_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add memory field to ProjectModel and wire into ProjectScanner</name>
  <files>
    pro_orc/lib/data/models/project_model.dart
    pro_orc/lib/data/services/project_scanner.dart
  </files>
  <action>
In `project_model.dart`:
- Add `import 'memory_data.dart';` (package import)
- Add field `final MemoryData? memory;` to ProjectModel (nullable, like gsd and git)
- Add `this.memory,` to constructor

In `project_scanner.dart`:
- Add `import 'memory_reader.dart';` (package import)
- In the `scanAll()` method, after step 4 (git data) and before step 5 (assemble), add a step to read memory data for all projects:
  ```dart
  // --- 4b. Read memory data for all projects ---
  final memoryResults = await Future.wait(
    projectPaths.map((path) => readMemoryData(path)),
  );
  ```
- In the step 5 loop where ProjectModel is assembled, add `memoryResults[i]` and pass it:
  ```dart
  final memoryData = memoryResults[i];
  ```
- In the ProjectModel constructor call, add:
  ```dart
  memory: memoryData.hasMemory ? memoryData : null,
  ```
  (Nullify when no memory exists, consistent with gsd/git pattern of null = not present)

Ensure `flutter analyze` passes with no new issues.
  </action>
  <verify>`flutter analyze` — no new issues. `flutter test test/data/project_scanner_test.dart` — existing tests still pass.</verify>
  <done>ProjectModel has memory field, ProjectScanner populates it during scanAll</done>
</task>

<task type="auto">
  <name>Task 2: Add integration test for memory detection in ProjectScanner</name>
  <files>
    pro_orc/test/data/project_scanner_test.dart
  </files>
  <action>
Read the existing project_scanner_test.dart to understand test setup (it uses createTempProject helper and scanDirOverride).

Add a new test group or test case within the existing structure:

Test: "scanned project includes memory data when MEMORY.md exists"
- Use existing `createTempProject()` helper to create a project in a temp scan dir
- Get the project's absolute path
- Create the Claude-style memory directory structure in a temp claude home:
  `{tempClaudeHome}/projects/{encodeProjectPath(projectPath)}/memory/MEMORY.md`
  Write some content to MEMORY.md
- NOTE: Since readMemoryData uses `~/.claude` by default and the override is on the function, not the scanner, there are two options:
  1. Set `HOME` env var (fragile, affects other tests)
  2. Create the memory file at the REAL `~/.claude/projects/...` path for the temp project (the encoded temp path will be unique, safe to create and clean up)

Choose option 2: Create the memory file at `~/.claude/projects/{encodedTempPath}/memory/MEMORY.md`, run scanAll, verify `project.memory != null && project.memory!.hasMemory == true`, then clean up the created dirs in tearDown.

If option 2 is too invasive (writing to real ~/.claude), instead add a test that directly calls `readMemoryData` with `claudeHomeDirOverride` — this is already tested in 12-01, so a simpler integration test is fine: just verify that after wiring, `ProjectModel.memory` field exists and is accessible (compile-time verification + one scan test that checks memory is null for a project with no memory file — which is the default case).

Simpler approach: Add one test that scans a temp project and asserts `project.memory == null` (no memory file exists for temp project path). This verifies the wiring works end-to-end without needing to set up Claude's directory structure.

Run all tests: `flutter test test/data/` — everything passes.
  </action>
  <verify>`flutter test test/data/` — all tests pass including new memory integration test</verify>
  <done>Integration test confirms ProjectScanner populates (or correctly nullifies) memory field during scan</done>
</task>

</tasks>

<verification>
- `flutter test` — all tests pass (existing + new)
- `flutter analyze` — clean
- ProjectModel.memory field accessible from UI layer (Phase 13 ready)
- ProjectScanner.scanAll() calls readMemoryData for each project
</verification>

<success_criteria>
- ProjectModel has `MemoryData? memory` field
- ProjectScanner calls readMemoryData in scanAll loop
- All existing tests still pass (no regression)
- Integration test verifies memory wiring
- Phase 13 can access `project.memory?.hasMemory`, `project.memory?.lastConsolidated`, `project.memory?.isStale`
</success_criteria>

<output>
After completion, create `.planning/phases/12-memory-detection/12-02-SUMMARY.md`
</output>
