---
phase: 11-claude-tools-panel
plan: "03"
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: []
autonomous: false
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04]

must_haves:
  truths:
    - "All Skills from ~/.claude/skills/ appear in the Skills section with correct name and description"
    - "All Plugins from installed_plugins.json appear in the Plugins section with name, version, and enabled status"
    - "MCP-Server section is visible with appropriate empty state"
    - "Search field filters all sections by name"
    - "Adding a new skill to ~/.claude/skills/ and waiting causes it to appear in the tab"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Claude Tools tab — all Phase 11 success criteria.

Purpose: Confirm the auto-discovery, display, live update, and interaction features work as specified before marking Phase 11 complete.

Output: Verified Phase 11 or list of issues to fix.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-claude-tools-panel/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of Phase 11 Claude Tools tab</name>
  <action>
    Verify the complete Claude Tools tab: auto-discovery of Skills, Plugins, and MCP servers from `~/.claude/`, three vertical sections with mini cards, search filtering, and live file-watching updates.
  </action>
  <what-built>
    Complete Claude Tools tab with auto-discovery of Skills, Plugins, and MCP servers from `~/.claude/`, three vertical sections with mini cards, search filtering, and live file-watching updates.
  </what-built>
  <how-to-verify>
    Run the app: `cd pro_orc && flutter run -d macos`

    **1. Skills Section (TOOL-01)**
    - Navigate to Claude Tools tab (third tab in NavigationRail)
    - Verify Skills section header shows correct count (e.g., "Skills (7)")
    - Verify each skill card shows name and description from SKILL.md
    - Verify cards are sorted alphabetically
    - Click Finder icon on a skill card → Finder opens to that skill's directory
    - If a skill has a homepage URL, click the browser icon → browser opens homepage

    **2. Plugins Section (TOOL-03)**
    - Verify Plugins section shows all installed plugins with name, version, and enabled/disabled status
    - Verify marketplace link button works (opens GitHub repo in browser)
    - Verify plugin descriptions are shown where available

    **3. MCP Server Section (TOOL-02)**
    - Verify MCP-Server section is visible with empty state text (no servers currently configured)
    - Section header should show "MCP-Server (0)"

    **4. Display Quality (TOOL-04)**
    - Verify each section uses its distinct accent color (amber=Skills, emerald=Plugins, violet=MCP)
    - Verify mini cards are compact and multiple fit per row
    - Verify text overflow is handled (ellipsis on long names/descriptions)
    - Verify glassmorphism style matches existing cards

    **5. Search**
    - Type in the search field — verify all three sections filter live by name
    - Clear search — verify all items reappear
    - Search for a skill name — verify only matching cards show, other sections show empty (but section headers remain visible)

    **6. Live Update**
    - Create a test skill directory: `mkdir -p ~/.claude/skills/test-verify && echo '---\nname: Test Skill\ndescription: Verification test\n---' > ~/.claude/skills/test-verify/SKILL.md`
    - Wait 1-2 seconds — verify the new skill appears in the Skills section without restarting the app
    - Clean up: `rm -rf ~/.claude/skills/test-verify`
    - Verify the skill disappears from the tab

    **7. Empty States**
    - If all sections had content: verify removing all would show full empty state (not required to test destructively — visual inspection of code logic suffices)

    **8. Overall**
    - Verify no console errors or Flutter warnings during normal interaction
    - Verify tab switching (Code → Research → Claude Tools) works smoothly
    - Verify scroll works when content exceeds viewport
  </how-to-verify>
  <resume-signal>Type "approved" if all areas pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 11 success criteria confirmed by human:
1. Claude Tools tab lists all Skills with correct name and description
2. MCP servers and Plugins discovered and displayed in distinct sections with type labels
3. Adding a new skill file causes it to appear after live refresh
</verification>

<success_criteria>
Human confirms all Phase 11 features work as specified in ROADMAP.md success criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/11-claude-tools-panel/11-03-SUMMARY.md`
</output>
