---
phase: 19-detail-panel-typography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pro_orc/lib/features/shared/project_detail_panel.dart
autonomous: false
requirements:
  - DPL-01
  - DPL-02
  - DPL-03
  - DPL-04

must_haves:
  truths:
    - "Beschreibungstexte im Detail-Panel haben Zeilenhoehe 1.6 und sind deutlich lesbarer"
    - "User kann Beschreibungstexte und Naechster-Schritt-Text mit Maus selektieren und per Cmd+C kopieren"
    - "Beschreibungstexte nutzen textSec (#9399A0) auf bgSurf — WCAG AA konform (~6:1)"
    - "Lange Beschreibungen (>5 Zeilen) sind eingeklappt mit 'Mehr anzeigen' Button; Klick expandiert, 'Weniger anzeigen' klappt zu"
    - "Kurze Beschreibungen (<= 5 Zeilen) sind immer voll sichtbar und selektierbar, ohne Toggle-Button"
  artifacts:
    - path: "pro_orc/lib/features/shared/project_detail_panel.dart"
      provides: "_DescriptionSection StatefulWidget with expand/collapse, _ExpandToggleButton with hover"
      contains: "_DescriptionSection"
  key_links:
    - from: "_buildBody in ProjectDetailPanel"
      to: "_DescriptionSection"
      via: "replaces inline _SectionCard+Text with _DescriptionSection widget"
      pattern: "_DescriptionSection\\("
    - from: "_DescriptionSection"
      to: "_ExpandToggleButton"
      via: "renders toggle button when text exceeds 5 lines"
      pattern: "_ExpandToggleButton\\("
    - from: "_buildBody NAECHSTER SCHRITT"
      to: "SelectableText"
      via: "Text replaced with SelectableText for copy support"
      pattern: "SelectableText\\(\\s*gsd!.nextStep"
---

<objective>
Beschreibungstexte im Detail-Panel lesbar, selektierbar und bei langen Texten ein-/ausklappbar machen. Naechster-Schritt-Text zusaetzlich selektierbar machen.

Purpose: Verbesserung der Lesbarkeit und Nutzbarkeit des Detail-Panels — Zeilenhoehe, Textauswahl, Kontrast und Expand/Collapse fuer lange Beschreibungen.
Output: Modifizierte `project_detail_panel.dart` mit zwei neuen privaten Widgets (`_DescriptionSection`, `_ExpandToggleButton`) und einem `Text` -> `SelectableText` Swap im Naechster-Schritt-Bereich.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-detail-panel-typography/19-CONTEXT.md
@.planning/phases/19-detail-panel-typography/19-RESEARCH.md
@pro_orc/lib/features/shared/project_detail_panel.dart

<interfaces>
<!-- Existing widgets and patterns in project_detail_panel.dart that the executor needs -->

From project_detail_panel.dart (line 582-629) — _SectionCard wrapper used by _DescriptionSection:
```dart
class _SectionCard extends StatelessWidget {
  const _SectionCard({
    required this.colors,
    required this.accent,
    required this.title,
    required this.child,
  });
  final AppColors colors;
  final Color accent;
  final String title;
  final Widget child;
  // Renders a styled container with title header + child content
}
```

From project_detail_panel.dart (line 632-741) — _DecisionsSection pattern to mirror:
```dart
class _DecisionsSection extends StatefulWidget {
  // bool _expanded toggle with setState — same pattern for _DescriptionSection
  // Uses LucideIcons.chevronDown100 / chevronUp100
}
```

From project_detail_panel.dart (line 226-236) — current BESCHREIBUNG to replace:
```dart
if (project.description != null)
  _SectionCard(
    colors: colors, accent: accent, title: 'BESCHREIBUNG',
    child: Text(
      project.description!,
      style: TextStyle(color: colors.textSec, fontSize: 14, height: 1.5),
    ),
  ),
```

From project_detail_panel.dart (line 217-219) — current NAECHSTER SCHRITT Text to swap:
```dart
Text(
  gsd!.nextStep!,
  style: TextStyle(color: colors.textPri, fontSize: 14),
),
```

Color tokens from AppColors (n3_colors.dart):
- `textSec` (#9399A0) — description text color (WCAG AA on bgSurf)
- `textPri` — primary text (nextStep)
- `textDim` (#5F6469) — toggle button default color
- `bgSurf` (#0A1017) — surface background
- accent: cyan (Code) or fuchsia (Research) — toggle button hover color
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SelectableText swap for NAECHSTER SCHRITT + _DescriptionSection with expand/collapse</name>
  <files>pro_orc/lib/features/shared/project_detail_panel.dart</files>
  <action>
All changes in `project_detail_panel.dart`. Three modifications:

**1. NAECHSTER SCHRITT — Text to SelectableText (line 217-219):**
Replace `Text(gsd!.nextStep!, ...)` with `SelectableText(gsd!.nextStep!, ...)` keeping the same style. No other changes to this section.

**2. Add `_ExpandToggleButton` StatefulWidget (place after `_DecisionsSection`, before `_LinkChip`):**
- Props: `bool expanded`, `AppColors colors`, `Color accent`, `VoidCallback onTap`
- Local `_hovered` state for hover effect
- `MouseRegion` wrapping `GestureDetector` with `SystemMouseCursors.click`
- Row with `Icon` (chevronRight100 when collapsed, chevronDown100 when expanded) + `Text`
- Labels: "Mehr anzeigen" (collapsed) / "Weniger anzeigen" (expanded)
- Color: `_hovered ? widget.accent : widget.colors.textDim`
- Font: fontSize 12, fontWeight w400
- Icon size: 14, SizedBox(width: 4) between icon and text
- Use exact icon names `LucideIcons.chevronRight100` and `LucideIcons.chevronDown100`

**3. Add `_DescriptionSection` StatefulWidget (place before `_ExpandToggleButton`):**
- Props: `AppColors colors`, `Color accent`, `String description`
- Local `_expanded` state, default `false`
- `_textStyle` getter: `TextStyle(color: widget.colors.textSec, fontSize: 14, height: 1.6)` (DPL-01: height 1.6)
- `_needsExpansion` computed as a getter using `TextPainter`:
  - Create TextPainter with text span, maxLines: 5, textDirection: ltr
  - Layout with maxWidth 624 (panel 700 - padding 76)
  - Read `didExceedMaxLines`, dispose TextPainter, return result
- Build method:
  - Compute `needsToggle = _needsExpansion`
  - Wrap in `_SectionCard(colors, accent, title: 'BESCHREIBUNG', child: Column(...))`
  - If `!needsToggle || _expanded`: render `SelectableText(description, style: _textStyle)` (DPL-02: selectable)
  - Else (long, collapsed): render `Text(description, style: _textStyle, maxLines: 5, overflow: TextOverflow.ellipsis)` (not selectable per user decision)
  - If `needsToggle`: render `Padding(top: 8)` with `_ExpandToggleButton(expanded, colors, accent, onTap: toggle)`

**4. Replace BESCHREIBUNG call site (line 226-236):**
Replace the existing inline `_SectionCard(... child: Text(project.description!, ...))` with:
```dart
if (project.description != null)
  _DescriptionSection(
    colors: colors,
    accent: accent,
    description: project.description!,
  ),
```

**Key constraints (from locked user decisions):**
- NO AnimatedContainer or CrossFade — instant toggle via bool
- NO LayoutBuilder — use hardcoded width 624 for TextPainter
- NO Markdown parsing — plain SelectableText only
- NO fade-out gradient — hard cut with ellipsis
- textSec color stays unchanged (DPL-03: already WCAG AA compliant at ~6:1)
- Collapsed long text uses `Text` (not SelectableText) because SelectableText doesn't support maxLines+ellipsis properly
  </action>
  <verify>
    <automated>cd /Users/rob/code/project_orchestration/pro_orc && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <done>
- NAECHSTER SCHRITT text is selectable (SelectableText widget)
- BESCHREIBUNG has height: 1.6 line spacing (DPL-01)
- BESCHREIBUNG is selectable when short or expanded (DPL-02)
- textSec color unchanged, WCAG AA compliant (DPL-03)
- Long descriptions show maxLines: 5 with ellipsis, "Mehr anzeigen" button below; expanded shows full text with "Weniger anzeigen" (DPL-04)
- Toggle button has hover effect (textDim -> accent color)
- flutter analyze passes with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visuelle Pruefung Detail-Panel Typography</name>
  <files>pro_orc/lib/features/shared/project_detail_panel.dart</files>
  <action>User verifies all typography improvements visually in the running app.</action>
  <what-built>Detail-Panel Typography: Zeilenhoehe 1.6, Selektierbarkeit, Expand/Collapse fuer lange Beschreibungen</what-built>
  <how-to-verify>
    1. `cd pro_orc && flutter run -d macos`
    2. Klick auf ein Projekt mit Beschreibung im Detail-Panel
    3. BESCHREIBUNG pruefen:
       - Zeilenabstand ist sichtbar groesser als vorher (1.6 statt 1.5)
       - Text mit Maus selektieren und Cmd+C kopieren — muss funktionieren
       - Text ist gut lesbar auf dunklem Hintergrund
    4. Projekt mit LANGER Beschreibung (>5 Zeilen) suchen oder testen:
       - Text ist auf 5 Zeilen abgeschnitten mit "..."
       - Darunter: Chevron-Icon + "Mehr anzeigen" in grauer Schrift
       - Hover ueber Button: Farbe wechselt zu Akzentfarbe (cyan/fuchsia)
       - Klick: Voller Text erscheint sofort, Button wechselt zu "Weniger anzeigen"
       - Aufgeklappter Text ist selektierbar
       - Klick auf "Weniger anzeigen": Text klappt wieder zu
    5. NAECHSTER SCHRITT pruefen: Text mit Maus selektierbar
    6. Projekt mit KURZER Beschreibung (<= 5 Zeilen): Kein Toggle-Button sichtbar, Text ist direkt selektierbar
  </how-to-verify>
  <verify>User confirms visual correctness</verify>
  <done>User approves all four DPL requirements are visually correct</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `flutter analyze` passes with zero errors
- _DescriptionSection widget exists with expand/collapse logic
- _ExpandToggleButton widget exists with hover effect
- NAECHSTER SCHRITT uses SelectableText
- BESCHREIBUNG uses height: 1.6 in TextStyle
- No new dependencies added to pubspec.yaml
</verification>

<success_criteria>
- All four requirements (DPL-01 through DPL-04) implemented in a single file change
- Description text has 1.6 line height, is selectable, meets WCAG AA contrast, and has expand/collapse for long text
- Next step text is selectable
- Zero new dependencies, zero analyzer errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-detail-panel-typography/19-01-SUMMARY.md`
</output>
