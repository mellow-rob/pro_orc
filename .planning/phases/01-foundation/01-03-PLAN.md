---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - pro-orc/app/layout.tsx
  - pro-orc/app/page.tsx
  - pro-orc/app/tools/page.tsx
autonomous: false
requirements: [DASH-06, INFRA-01, INFRA-02, INFRA-03, INFRA-04]

must_haves:
  truths:
    - "Opening localhost:3000 shows a dark-background page (ultra-dark navy, not white)"
    - "The page renders with Inter font (sans-serif) and correct antialiasing"
    - "The HTML element has class='dark' hardcoded — no toggle, no next-themes, no flash"
    - "The /tools route returns a 200 response (placeholder page ready for Phase 5)"
    - "No authentication prompt, no login page, no database connection errors"
  artifacts:
    - path: "pro-orc/app/layout.tsx"
      provides: "Root layout with dark class and font variables"
      contains: "class=\"dark\""
    - path: "pro-orc/app/page.tsx"
      provides: "Dashboard placeholder — dark background with Pro Orc title"
    - path: "pro-orc/app/tools/page.tsx"
      provides: "Tools page placeholder for Phase 5"
  key_links:
    - from: "pro-orc/app/layout.tsx"
      to: "app/globals.css dark theme"
      via: "class=\"dark\" on html element + CSS @custom-variant dark"
      pattern: "className.*dark"
    - from: "pro-orc/app/layout.tsx"
      to: "next/font/google Inter + JetBrains_Mono"
      via: "CSS variable injection on body"
      pattern: "font-inter|font-mono"
---

<objective>
Wire the root layout with dark mode class and fonts, and create placeholder pages that prove the visual foundation works.

Purpose: Verify end-to-end that the dark theme, fonts, and route structure all render correctly before Phase 2 builds data features on top of them.
Output: A running localhost:3000 showing ultra-dark background with Pro Orc title, Inter font active, and both / and /tools routes returning 200.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root layout and placeholder pages</name>
  <files>
    pro-orc/app/layout.tsx
    pro-orc/app/page.tsx
    pro-orc/app/tools/page.tsx
  </files>
  <action>
    Working directory: /Users/rob/project_orchestration/pro-orc/

    Step 1 — Write app/layout.tsx:
    - Inter + JetBrains_Mono loaded via next/font/google with CSS variables
    - `class="dark"` hardcoded on `<html>` — no next-themes, no dynamic class, no flash
    - Body gets both font variables and antialiased class
    - No authentication checks, no middleware calls, no redirects

    ```typescript
    // app/layout.tsx
    import type { Metadata } from 'next'
    import { Inter, JetBrains_Mono } from 'next/font/google'
    import './globals.css'

    const inter = Inter({
      subsets: ['latin'],
      variable: '--font-inter',
      display: 'swap',
    })

    const jetbrainsMono = JetBrains_Mono({
      subsets: ['latin'],
      variable: '--font-mono',
      display: 'swap',
    })

    export const metadata: Metadata = {
      title: 'Pro Orc',
      description: 'Project Orchestration Dashboard',
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        // dark class hardcoded — no toggle, no next-themes, no flash of light mode
        <html lang="en" className="dark">
          <body
            className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased bg-background text-foreground`}
          >
            {children}
          </body>
        </html>
      )
    }
    ```

    Step 2 — Write app/page.tsx (dashboard placeholder):
    - Dark background, Pro Orc title with cyan accent
    - Atmospheric blur orbs in background (n3urala1 style)
    - Import and use cn() from @/lib/utils — validates shadcn utils work
    - Import types from @/lib/types — validates types.ts import path works
    - Placeholder text that will be replaced in Phase 3

    ```typescript
    // app/page.tsx
    import { cn } from '@/lib/utils'
    import type { Project } from '@/lib/types'

    export default function DashboardPage() {
      // Phase 3 will replace this with real project data from /api/projects
      const projects: Project[] = []

      return (
        <main className="relative min-h-screen overflow-hidden">
          {/* Atmospheric background orbs — n3urala1 aesthetic */}
          <div
            aria-hidden="true"
            className={cn(
              'pointer-events-none absolute -top-40 -left-40 h-[600px] w-[600px] rounded-full',
              'bg-orb-cyan'
            )}
          />
          <div
            aria-hidden="true"
            className={cn(
              'pointer-events-none absolute -bottom-40 -right-40 h-[600px] w-[600px] rounded-full',
              'bg-orb-fuchsia'
            )}
          />

          {/* Content */}
          <div className="relative z-10 flex min-h-screen flex-col items-center justify-center gap-4 p-8">
            <h1 className="font-sans text-4xl font-bold tracking-tighter text-foreground">
              Pro{' '}
              <span className="text-primary">Orc</span>
            </h1>
            <p className="text-muted-foreground">
              Project Orchestration Dashboard
            </p>
            <p className="font-mono text-xs text-muted-foreground/60">
              {/* Phase 3 will replace this with real project count */}
              {projects.length} projects — data layer coming in Phase 2
            </p>
          </div>
        </main>
      )
    }
    ```

    Step 3 — Create app/tools/ directory and page.tsx:
    ```typescript
    // app/tools/page.tsx
    export default function ToolsPage() {
      return (
        <main className="flex min-h-screen items-center justify-center">
          <div className="text-center">
            <h1 className="font-sans text-2xl font-bold text-foreground">
              Claude Tools
            </h1>
            <p className="mt-2 text-muted-foreground">
              Phase 5 — Skills, MCP servers, and Plugins
            </p>
          </div>
        </main>
      )
    }
    ```

    Step 4 — Run TypeScript check:
    ```bash
    npx tsc --noEmit
    ```
    All three files must compile without errors.

    Do NOT: add any auth wrapper, any API calls, any use client directive (these are server components).
    Do NOT: import from next-themes or any auth library.
  </action>
  <verify>
    ```bash
    cd /Users/rob/project_orchestration/pro-orc
    # dark class is hardcoded
    grep 'className="dark"' app/layout.tsx
    # Both fonts loaded
    grep "Inter\|JetBrains_Mono" app/layout.tsx | head -4
    # Types imported in page.tsx (validates @/lib/types path works)
    grep "@/lib/types" app/page.tsx
    # Tools page exists
    cat app/tools/page.tsx | head -5
    # Full TypeScript check
    npx tsc --noEmit 2>&1 | tail -5
    ```
    Expected: dark class present, both fonts imported, @/lib/types import in page, tools page exists, zero TS errors.
  </verify>
  <done>
    - app/layout.tsx: `<html className="dark">`, Inter + JetBrains_Mono with CSS variables, antialiased body
    - app/page.tsx: imports from @/lib/utils and @/lib/types, renders with dark background and Pro Orc title
    - app/tools/page.tsx: placeholder page for Phase 5 tools inventory
    - npx tsc --noEmit exits 0
    - No next-themes, no auth, no 'use client' on layout
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dark theme and route structure</name>
  <files>pro-orc/app/layout.tsx</files>
  <action>
    CHECKPOINT — Human visual verification required.

    What was built across Plans 01-03:
    - Next.js 16 project (pro-orc/) with serverExternalPackages for chokidar, simple-git, fsevents
    - lib/types.ts with full discriminated union type system
    - lib/paths.ts with os.homedir() path constants
    - shadcn/ui New York style with n3urala1 dark theme (Cyan primary, Fuchsia accent)
    - app/layout.tsx with hardcoded dark class and Inter + JetBrains Mono fonts
    - app/page.tsx with dark background, atmospheric blur orbs, Pro Orc title
    - app/tools/page.tsx placeholder

    User verification steps:
    1. Start dev server: `cd /Users/rob/project_orchestration/pro-orc && npm run dev`
    2. Open http://localhost:3000 — confirm ultra-dark navy background, "Pro Orc" title with cyan "Orc", atmospheric blur orbs
    3. Open DevTools → Elements: confirm `<html class="dark">` and body has --font-inter CSS variable
    4. Open http://localhost:3000/tools — confirm "Claude Tools" heading on dark background
    5. Check browser console — must be completely clean (no red errors)

    Resume: Type "approved" if the dark theme renders correctly, or describe any issues.
  </action>
  <verify>
    Human visual confirmation — the dark theme renders as intended:
    - Background: ultra-dark navy (oklch 0.11 0.02 264, approximately #0a0f1a)
    - "Orc" text: cyan (#06b6d4)
    - No white background flash, no gray background
    - Inter font antialiased
    - Browser console clean
  </verify>
  <done>
    User has confirmed "approved" — dark theme, fonts, and route structure all render correctly at localhost:3000.
    Phase 1 complete: foundation is solid for Phase 2 data layer work.
  </done>
</task>

</tasks>

<verification>
From /Users/rob/project_orchestration/pro-orc/:

```bash
# Structure check
ls app/layout.tsx app/page.tsx app/tools/page.tsx lib/types.ts lib/paths.ts next.config.ts instrumentation.ts

# Phase 1 success criteria
grep "serverExternalPackages" next.config.ts
grep 'className="dark"' app/layout.tsx
grep "@custom-variant dark" app/globals.css
npx tsc --noEmit 2>&1 | tail -3

# No forbidden packages
grep -E '"prisma"|"next-themes"|"@auth|"tailwindcss-animate"' package.json || echo "CLEAN"
```
</verification>

<success_criteria>
All five phase success criteria (from ROADMAP.md):

1. `npm run dev` starts at localhost:3000 with no network binding — INFRA-01
2. App renders dark-background page with correct Tailwind v4 CSS variables applied — DASH-06
3. next.config.ts includes serverExternalPackages for chokidar, simple-git, fsevents — INFRA-01
4. All shared TypeScript types (ProjectData, ResearchProject, GsdStatus, etc.) defined in lib/types.ts and import cleanly — INFRA-04
5. No database, no auth, no hardcoded paths — all resolved via os.homedir() — INFRA-02, INFRA-03, INFRA-04

Visual: Ultra-dark navy background with cyan "Orc" accent, atmospheric blur orbs, Inter font. Exactly the n3urala1.com aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
