---
phase: 25-open-source-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - CONTRIBUTING.md
  - README.md
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/pull_request_template.md
  - pro_orc/lib/data/services/memory_reader.dart
  - pro_orc/README.md
autonomous: true
requirements: [OSS-01, OSS-02, OSS-03, OSS-04]

must_haves:
  truths:
    - "MIT LICENSE Datei liegt im Repo-Root mit Copyright 2026 mellow-rob"
    - "CONTRIBUTING.md erklaert Prerequisites, Build/Test Commands und PR Process"
    - "README.md enthaelt aktuelle Feature-Liste inkl. Claude-Button, Onboarding, Plugin Browser"
    - "README.md Lizenz-Section zeigt MIT License statt Private project"
    - "GitHub Issue Templates fuer Bug Report und Feature Request existieren"
    - "GitHub PR Template mit Checklist existiert"
    - "Keine hardcoded /Users/rob Pfade im Source Code (auch nicht in Kommentaren)"
  artifacts:
    - path: "LICENSE"
      provides: "MIT License text"
      contains: "MIT License"
    - path: "CONTRIBUTING.md"
      provides: "Contribution guidelines"
      contains: "flutter test"
    - path: "README.md"
      provides: "Updated project README with current features"
      contains: "Claude-Button"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Bug report issue template"
      contains: "bug"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Feature request issue template"
      contains: "feature"
    - path: ".github/pull_request_template.md"
      provides: "PR template with checklist"
      contains: "flutter analyze"
---

<objective>
Open Source Release vorbereiten: LICENSE, CONTRIBUTING, GitHub Templates, README-Update, und Repo-Audit.

Purpose: OSS-01/02/03/04 — Pro Orc ist bereit fuer die oeffentliche GitHub-Veroeffentlichung mit professioneller Dokumentation und sauberem Repo.
Output: LICENSE, CONTRIBUTING.md, issue/PR templates, updated README, cleaned repo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-open-source-polish/25-CONTEXT.md

<interfaces>
<!-- Existing files to modify -->

From README.md (line 96):
```markdown
## License

Private project — not licensed for distribution.
```

From README.md (lines 5-16) — current features list:
```markdown
## Features

- **Auto-scan** configurable directories for projects (code and research)
- **GSD status** at a glance — phase progress, completion percentage, next steps
- **Git integration** — last commit, branch, dirty state, GitHub links
- **Claude Tools inventory** — discovers installed Skills, Plugins, and MCP servers from `~/.claude/`
- **Memory indicator** — shows Claude rem-sleep memory consolidation status per project
- **Quick actions** — open in Terminal, Finder, GitHub; right-click context menus
- **Menubar-only** — lives in the macOS menubar, no Dock icon
- **Reactive** — file watcher auto-refreshes when projects change on disk
- **Private projects** — hide projects from the main view, toggle visibility
```

From README.md (lines 41-47) — current Getting Started:
```markdown
## Getting Started

1. Launch Pro Orc — a menubar icon appears
2. Open **Settings** (gear icon in the navigation rail)
3. Add your project directories (e.g. `~/code`, `~/research`)
4. Projects appear automatically in the **Code** and **Research** tabs
5. Browse installed Claude tools in the **Claude Tools** tab
```

From .github/workflows/release.yml — already exists, no changes needed.

From pro_orc/lib/data/services/memory_reader.dart — has /Users/rob in comments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LICENSE and CONTRIBUTING.md</name>
  <files>
    LICENSE,
    CONTRIBUTING.md
  </files>
  <action>
    1. Create `LICENSE` in repo root with standard MIT License text:
       - Year: 2026
       - Copyright holder: mellow-rob
       - Full MIT License text (no modifications)

    2. Create `CONTRIBUTING.md` in repo root (English):
       ```markdown
       # Contributing to Pro Orc

       Thanks for your interest in contributing! ...

       ## Prerequisites

       - macOS (Pro Orc is a native macOS app)
       - Flutter SDK (stable channel)
       - Xcode Command Line Tools

       ## Development Setup

       git clone + cd + flutter run -d macos

       ## Running Tests

       flutter test, flutter analyze

       ## Code Style

       - Package imports only (no relative imports)
       - Immutable patterns (no mutation)
       - German UI strings
       - See CLAUDE.md for architecture details

       ## Pull Request Process

       1. Fork the repo
       2. Create a feature branch
       3. Run `flutter test` and `flutter analyze`
       4. Submit PR with description of changes

       ## Reporting Issues

       Use GitHub Issues with the provided templates.
       ```
  </action>
  <verify>
    <automated>test -f /Users/rob/code/project_orchestration/LICENSE && test -f /Users/rob/code/project_orchestration/CONTRIBUTING.md && echo "OK"</automated>
  </verify>
  <done>
    - MIT LICENSE exists in repo root
    - CONTRIBUTING.md with prerequisites, dev setup, PR process
  </done>
</task>

<task type="auto">
  <name>Task 2: GitHub Issue and PR Templates</name>
  <files>
    .github/ISSUE_TEMPLATE/bug_report.yml,
    .github/ISSUE_TEMPLATE/feature_request.yml,
    .github/pull_request_template.md
  </files>
  <action>
    1. Create `.github/ISSUE_TEMPLATE/bug_report.yml`:
       - YAML format (GitHub Forms)
       - Fields: description, steps to reproduce, expected behavior, actual behavior, macOS version, Pro Orc version
       - Labels: ["bug"]

    2. Create `.github/ISSUE_TEMPLATE/feature_request.yml`:
       - YAML format (GitHub Forms)
       - Fields: description, use case, proposed solution (optional)
       - Labels: ["enhancement"]

    3. Create `.github/pull_request_template.md`:
       ```markdown
       ## Description

       <!-- What does this PR do? -->

       ## Checklist

       - [ ] `flutter test` passes
       - [ ] `flutter analyze` shows no issues
       - [ ] Code follows project conventions (see CLAUDE.md)
       ```
  </action>
  <verify>
    <automated>test -f /Users/rob/code/project_orchestration/.github/ISSUE_TEMPLATE/bug_report.yml && test -f /Users/rob/code/project_orchestration/.github/ISSUE_TEMPLATE/feature_request.yml && test -f /Users/rob/code/project_orchestration/.github/pull_request_template.md && echo "OK"</automated>
  </verify>
  <done>
    - Bug report and feature request issue templates in YAML format
    - PR template with test/analyze checklist
  </done>
</task>

<task type="auto">
  <name>Task 3: README Update and Repo Cleanup</name>
  <files>
    README.md,
    pro_orc/lib/data/services/memory_reader.dart,
    pro_orc/README.md
  </files>
  <action>
    1. Update `README.md` features list — add after existing items:
       - **Claude-Button** — start Claude Code sessions directly from project cards
       - **Onboarding wizard** — first-run setup with Claude Code detection and directory configuration
       - **Skill/Plugin browser** — view per-project skills, plugins, and MCP servers with metadata
       Update "Quick actions" line to mention Claude-Button as primary action.

    2. Update `README.md` Getting Started section:
       - Step 2: Mention that on first launch, a setup wizard guides through configuration
       - Keep manual Settings path as fallback

    3. Replace `README.md` License section (line 96):
       ```markdown
       ## License

       [MIT](LICENSE)
       ```

    4. Clean hardcoded paths in `pro_orc/lib/data/services/memory_reader.dart`:
       - Replace any `/Users/rob` in comments with generic `~/` or `$HOME`
       - Do NOT change any code logic, only comments

    5. Delete or replace `pro_orc/README.md`:
       - Replace Flutter boilerplate with single line: "See [root README](../README.md) for project documentation."
  </action>
  <verify>
    <automated>grep -r "/Users/rob" /Users/rob/code/project_orchestration/pro_orc/lib/ 2>/dev/null | grep -v ".g.dart" | wc -l | tr -d ' ' | grep -q "^0$" && grep "MIT" /Users/rob/code/project_orchestration/README.md && echo "OK"</automated>
  </verify>
  <done>
    - README features updated with v2.0 additions
    - License section shows MIT
    - No hardcoded /Users/rob in lib/ source
    - pro_orc/README.md cleaned
  </done>
</task>

</tasks>

<verification>
```bash
# Files exist
test -f LICENSE && test -f CONTRIBUTING.md && echo "License+Contributing OK"
test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && echo "Templates OK"
test -f .github/pull_request_template.md && echo "PR Template OK"

# No hardcoded paths
grep -r "/Users/rob" pro_orc/lib/ 2>/dev/null | grep -v ".g.dart" | wc -l
# Expected: 0

# License in README
grep "MIT" README.md

# Features in README
grep "Claude-Button" README.md
grep "Onboarding" README.md

# Tests still pass
cd pro_orc && flutter test && flutter analyze
```
</verification>

<success_criteria>
- OSS-01: README erklaert Features (inkl. v2.0), enthaelt Installationsanleitung und Quick-Start
- OSS-02: LICENSE (MIT) und CONTRIBUTING.md liegen im Repo-Root
- OSS-03: Keine hardcoded /Users/rob im Source Code, .gitignore ist vollstaendig
- OSS-04: GitHub Issue Templates und PR Template sind konfiguriert
- Alle Tests weiterhin gruen, Analyzer sauber
</success_criteria>

<output>
After completion, create `.planning/phases/25-open-source-polish/25-01-SUMMARY.md`
</output>
