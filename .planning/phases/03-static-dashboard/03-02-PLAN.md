---
phase: 03-static-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - pro-orc/app/page.tsx
autonomous: false
requirements:
  - DASH-01

must_haves:
  truths:
    - "Opening localhost:3000 shows a card grid with all auto-discovered projects"
    - "Code projects render in CodeProjectCard layout with all GSD fields"
    - "Research projects render in ResearchProjectCard layout with no git data"
    - "Grid is responsive: 1 col mobile, 2 col md, 3 col lg"
  artifacts:
    - path: "pro-orc/app/page.tsx"
      provides: "Dashboard page with async data fetch and card grid"
      contains: "scanProjects"
  key_links:
    - from: "pro-orc/app/page.tsx"
      to: "pro-orc/lib/scanner.ts"
      via: "direct import and await in async Server Component"
      pattern: "await scanProjects\\(\\)"
    - from: "pro-orc/app/page.tsx"
      to: "pro-orc/components/codeProjectCard.tsx"
      via: "import and render for code projects"
      pattern: "CodeProjectCard.*project="
    - from: "pro-orc/app/page.tsx"
      to: "pro-orc/components/researchProjectCard.tsx"
      via: "import and render for research projects"
      pattern: "ResearchProjectCard.*project="
---

<objective>
Wire the dashboard page to fetch real project data and render the card grid.

Purpose: Replace the Phase 1 placeholder page.tsx with an async Server Component that calls scanProjects() directly and renders CodeProjectCard/ResearchProjectCard in a responsive grid. This completes the static dashboard — the core value proposition of Phase 3.

Output: Rewritten page.tsx as async Server Component with real data
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-dashboard/03-RESEARCH.md
@.planning/phases/03-static-dashboard/03-01-SUMMARY.md

@pro-orc/app/page.tsx
@pro-orc/lib/scanner.ts
@pro-orc/lib/types.ts
@pro-orc/components/codeProjectCard.tsx
@pro-orc/components/researchProjectCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite page.tsx as async Server Component with card grid</name>
  <files>pro-orc/app/page.tsx</files>
  <action>
Replace the placeholder page.tsx with an async Server Component:

1. Import `scanProjects` from `@/lib/scanner`, `isCodeProject` from `@/lib/types`, `CodeProjectCard` from `@/components/codeProjectCard`, `ResearchProjectCard` from `@/components/researchProjectCard`.

2. Make the default export an `async function DashboardPage()`.

3. Call `const projects = await scanProjects()` at the top — direct function call, NO fetch to an API route.

4. Compute counts: `codeProjects = projects.filter(isCodeProject)`, `researchProjects` for the rest.

5. Keep the atmospheric orbs from the Phase 1 placeholder (bg-orb-cyan, bg-orb-fuchsia with same positioning and aria-hidden).

6. Render a header section with:
   - "Pro Orc" title (same styling as placeholder: text-3xl or text-4xl font-bold tracking-tighter, "Orc" in text-primary)
   - Subtitle line with project counts in mono text: "{total} projects — {code} code · {research} research"

7. Render a responsive card grid: `grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3`

8. Map over `projects` array. For each project, use `isCodeProject(project)` to render either `<CodeProjectCard key={project.id} project={project} />` or `<ResearchProjectCard key={project.id} project={project} />`.

9. Content wrapper has `relative z-10 p-8` to sit above the orbs.

Do NOT add `export const dynamic = 'force-static'` — the page must fetch fresh data on each request. Do NOT create an API route — direct import is correct for Phase 3.
  </action>
  <verify>
`npm run dev` starts without errors. Opening http://localhost:3000 renders the dashboard. `npx tsc --noEmit` passes.
  </verify>
  <done>
page.tsx is an async Server Component calling scanProjects() directly. Projects render in a responsive grid with correct card types. Header shows project counts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete dashboard</name>
  <files>pro-orc/app/page.tsx</files>
  <action>
CHECKPOINT — No code changes. Visual and functional verification of the complete static dashboard.

What was built: Async Server Component page calling scanProjects(), rendering CodeProjectCard and ResearchProjectCard in a responsive grid with glassmorphism styling, GSD status badges, progress bars, timestamps, stale indicators, and quick action buttons.
  </action>
  <verify>
1. Open http://localhost:3000 in your browser
2. Verify card grid shows all discovered projects from ~/project_orchestration/code/ and ~/project_orchestration/project research/
3. For code project cards, verify:
   - Project name and GSD status badge visible
   - Current phase text shown (if project has .planning/)
   - Progress bar shown (if project has roadmap checkboxes)
   - Next step text shown (if available)
   - Last git activity timestamp shown as relative time ("X days ago")
   - "Terminal" and "Finder" buttons visible
   - Click "Terminal" — should open project folder in Terminal.app
   - Click "Finder" — should open project folder in Finder
4. For research project cards, verify:
   - Shows name, GSD status, Notion link (if available)
   - Does NOT show git timestamps, commits, or progress bar
   - "Open Notion" button opens the Notion URL in browser (if notionUrl exists)
5. Check for stale indicators: any project with no commit in 30+ days should show amber "Stale" badge
6. Verify responsive layout: resize browser — 1 column on small, 2 on medium, 3 on large
7. Verify glassmorphism: cards should be semi-transparent with blur orbs visible behind them
  </verify>
  <done>
User confirms dashboard renders correctly with all project cards, GSD data, quick actions, stale indicators, and responsive layout matching n3urala1 aesthetic.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts cleanly at localhost:3000
2. Dashboard shows all projects from both code/ and research/ directories
3. Code cards display all required fields (name, status, phase, progress, timestamp, next step)
4. Research cards display name, status, Notion link — no git data
5. Quick actions work: Terminal opens Terminal.app, Finder opens Finder, Notion opens browser
6. Stale projects marked with amber badge
7. Responsive grid layout works across viewport sizes
8. Glassmorphism card styling with n3urala1 aesthetic
</verification>

<success_criteria>
The dashboard at localhost:3000 renders a complete read-only view of all projects. Every code project shows its GSD data, git activity, and quick action buttons. Research projects show their distinct layout with Notion links. The visual design matches the n3urala1 aesthetic with glassmorphism cards, cyan/fuchsia accents, and atmospheric blur orbs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-static-dashboard/03-02-SUMMARY.md`
</output>
