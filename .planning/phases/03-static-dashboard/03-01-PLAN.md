---
phase: 03-static-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pro-orc/app/actions.ts
  - pro-orc/components/statusBadge.tsx
  - pro-orc/components/codeProjectCard.tsx
  - pro-orc/components/researchProjectCard.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-07
  - DASH-08
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
  - RSRCH-01
  - RSRCH-02
  - RSRCH-03

must_haves:
  truths:
    - "Code project card renders name, GSD status badge, phase text, progress bar, next step, and git timestamp"
    - "Research project card renders name, GSD status badge, Notion link, and no git metrics"
    - "Stale projects (30+ days no commit) show amber Stale badge"
    - "Open in Terminal button triggers server action to open Terminal.app"
    - "Open in Finder button triggers server action to open Finder"
    - "Open Notion Page link opens Notion URL in browser"
  artifacts:
    - path: "pro-orc/app/actions.ts"
      provides: "Server Actions for macOS open commands"
      exports: ["openInTerminal", "openInFinder", "openNotionPage"]
    - path: "pro-orc/components/statusBadge.tsx"
      provides: "Shared GSD status badge with color-coded styling"
      exports: ["StatusBadge"]
    - path: "pro-orc/components/codeProjectCard.tsx"
      provides: "Code project card with all data fields and quick actions"
      exports: ["CodeProjectCard"]
    - path: "pro-orc/components/researchProjectCard.tsx"
      provides: "Research project card with name, status, Notion link"
      exports: ["ResearchProjectCard"]
  key_links:
    - from: "pro-orc/components/codeProjectCard.tsx"
      to: "pro-orc/app/actions.ts"
      via: "import { openInTerminal, openInFinder } and call via startTransition"
      pattern: "startTransition.*openIn(Terminal|Finder)"
    - from: "pro-orc/components/codeProjectCard.tsx"
      to: "pro-orc/components/statusBadge.tsx"
      via: "import { StatusBadge }"
      pattern: "StatusBadge.*status"
    - from: "pro-orc/components/researchProjectCard.tsx"
      to: "pro-orc/components/statusBadge.tsx"
      via: "import { StatusBadge }"
      pattern: "StatusBadge.*status"
---

<objective>
Create all dashboard card components, shared utilities, and server actions for Phase 3.

Purpose: Build every component the dashboard page needs — two card types, status badge, server actions for macOS quick actions, and helper functions (formatRelativeTime, isStale) — so Plan 02 can simply wire them into page.tsx.

Output: 4 new files: actions.ts, statusBadge.tsx, codeProjectCard.tsx, researchProjectCard.tsx
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-dashboard/03-RESEARCH.md

@pro-orc/lib/types.ts
@pro-orc/lib/utils.ts
@pro-orc/components/ui/card.tsx
@pro-orc/components/ui/badge.tsx
@pro-orc/components/ui/button.tsx
@pro-orc/components/ui/progress.tsx
@pro-orc/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server actions and shared StatusBadge component</name>
  <files>
    pro-orc/app/actions.ts
    pro-orc/components/statusBadge.tsx
  </files>
  <action>
Create `app/actions.ts` with `'use server'` directive. Implement three exported async functions:

1. `openInTerminal(projectPath: string): Promise<void>` — validates path starts with `/Users/` and has no `..`, escapes single quotes, runs `exec('open -a Terminal '${safePath}'')` via `promisify(exec)` from `child_process`/`util`.

2. `openInFinder(projectPath: string): Promise<void>` — same validation, runs `exec('open '${safePath}'')`.

3. `openNotionPage(notionUrl: string): Promise<void>` — validates URL starts with `https://` or `http://`, runs `exec('open '${safeUrl}'')`. Note: research recommends `<a href>` for Notion (simpler), but provide the server action for consistency with ACT-03. The research card will use `<a href>` instead.

Do NOT add `child_process` to `serverExternalPackages` — it's a Node built-in.

Create `components/statusBadge.tsx` (no `'use client'` needed — it's a pure render component, can work in both server and client contexts since it will be imported by client components which makes it client automatically):

- Export `StatusBadge` component accepting `{ status: string }` prop
- Use a `STATUS_STYLES` lookup object mapping known GSD statuses to border/text color classes:
  - `building`: `border-primary/50 text-primary` (cyan)
  - `done`: `border-green-500/50 text-green-400`
  - `paused`: `border-amber-500/50 text-amber-400`
  - `research`: `border-accent/50 text-accent` (fuchsia)
  - `planning`: `border-blue-500/50 text-blue-400`
  - `archived`: `border-border text-muted-foreground`
  - Fallback for unknown: `border-border text-muted-foreground`
- Render shadcn `Badge` with `variant="outline"` and `cn('text-xs capitalize', style)`
  </action>
  <verify>
`npx tsc --noEmit` passes with no errors on the new files. Verify actions.ts exports three functions and statusBadge.tsx exports StatusBadge.
  </verify>
  <done>
actions.ts exports openInTerminal, openInFinder, openNotionPage with path/URL validation. statusBadge.tsx exports StatusBadge rendering colored outline badges for each known GSD status.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CodeProjectCard and ResearchProjectCard components</name>
  <files>
    pro-orc/components/codeProjectCard.tsx
    pro-orc/components/researchProjectCard.tsx
  </files>
  <action>
Create `components/codeProjectCard.tsx` as a `'use client'` component:

- Import `useTransition` from React, icons from `lucide-react` (Terminal, Folder, Clock), shadcn components (Card, CardHeader, CardTitle, CardContent, CardFooter, Badge, Button, Progress), server actions (openInTerminal, openInFinder), StatusBadge, CodeProject type, and `cn`.

- Include a local `isStale(timestamp?: string): boolean` helper — returns true if timestamp is older than 30 days (30 * 24 * 60 * 60 * 1000 ms). Returns false for undefined.

- Include a local `formatRelativeTime(isoTimestamp: string): string` helper using `Intl.RelativeTimeFormat('en', { numeric: 'auto' })`. Handles years, months, days, hours, minutes, and "just now".

- `CodeProjectCard` component receives `{ project: CodeProject }`:
  - Uses `useTransition` for isPending/startTransition
  - Computes `stale = isStale(project.lastCommitTimestamp)`
  - Card has `backdrop-blur-sm transition-shadow duration-200` + conditional `border-amber-500/30` if stale + `hover:glow-cyan`
  - CardHeader: project name as CardTitle, stale Badge (amber, "Stale") if stale, StatusBadge if gsdStatus exists, currentPhase in mono text below title
  - CardContent: Progress bar guarded by `phaseProgress !== undefined` with percentage label, nextStep as line-clamped text, lastCommitTimestamp with Clock icon in mono text using formatRelativeTime
  - CardFooter: "Terminal" button (outline, sm) calling `startTransition(() => openInTerminal(project.path))`, "Finder" button (ghost, sm) calling `startTransition(() => openInFinder(project.path))`. Both disabled when isPending.

Create `components/researchProjectCard.tsx` as a `'use client'` component:

- Import icons (ExternalLink, BookOpen), shadcn components (Card, CardHeader, CardTitle, CardContent, CardFooter, Button), StatusBadge, ResearchProject type, cn.

- `ResearchProjectCard` receives `{ project: ResearchProject }`:
  - Card has `border-accent/20 backdrop-blur-sm transition-shadow duration-200 hover:glow-fuchsia`
  - CardHeader: BookOpen icon (text-accent) + project name, StatusBadge if gsdStatus
  - CardContent (conditional): nextStep if present
  - CardFooter (conditional): "Open Notion" button as `<a href={project.notionUrl} target="_blank" rel="noopener noreferrer">` using shadcn Button with `asChild` prop, ExternalLink icon. Only render if notionUrl exists.
  - NO git timestamps, NO commit info, NO progress bar (RSRCH-03)
  </action>
  <verify>
`npx tsc --noEmit` passes. Both components export named functions. CodeProjectCard imports from actions.ts and statusBadge.tsx. ResearchProjectCard imports from statusBadge.tsx. Neither component references `lastCommitTimestamp` or git data in the research card.
  </verify>
  <done>
CodeProjectCard displays all DASH-02 through DASH-08 fields with stale detection and quick actions. ResearchProjectCard displays name, status, Notion link per RSRCH-01/02/03 with no git data. Both use glassmorphism styling with appropriate glow effects.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — all 4 new files compile without errors
2. Verify exports: actions.ts (3 server actions), statusBadge.tsx (StatusBadge), codeProjectCard.tsx (CodeProjectCard), researchProjectCard.tsx (ResearchProjectCard)
3. Verify codeProjectCard imports openInTerminal/openInFinder from actions.ts
4. Verify researchProjectCard does NOT import any git-related types or display git data
5. Verify 'use server' directive in actions.ts, 'use client' in both card components
</verification>

<success_criteria>
All 4 component files compile cleanly. Card components use the exact shadcn components and CSS utilities from Phase 1. Server actions use promisify(exec) with path validation. StatusBadge maps all known GSD statuses to appropriate colors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-static-dashboard/03-01-SUMMARY.md`
</output>
