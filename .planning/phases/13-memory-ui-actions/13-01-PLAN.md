---
phase: 13-memory-ui-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pro_orc/lib/features/shared/memory_indicator.dart
  - pro_orc/lib/features/code/code_project_card.dart
  - pro_orc/lib/features/research/research_project_card.dart
  - pro_orc/lib/data/services/quick_actions_service.dart
autonomous: true
requirements: [MUI-01, MUI-02, MUI-03, MACT-01]

must_haves:
  truths:
    - "Sleeping-book icon appears on every Code and Research project card"
    - "Icon is colored (violet) when memory exists and fresh, amber when stale, dim/gray when no memory"
    - "Tooltip shows 'Letzte Konsolidierung: DD.MM.YYYY' when memory exists, 'Keine Memory vorhanden' when not"
    - "Quick action button opens Terminal with claude CLI in project directory"
  artifacts:
    - path: "pro_orc/lib/features/shared/memory_indicator.dart"
      provides: "Reusable MemoryIndicator widget (icon + tooltip + 3 visual states)"
      min_lines: 30
    - path: "pro_orc/lib/features/code/code_project_card.dart"
      provides: "CodeProjectCard with memory indicator in title row"
      contains: "MemoryIndicator"
    - path: "pro_orc/lib/features/research/research_project_card.dart"
      provides: "ResearchProjectCard with memory indicator in title row"
      contains: "MemoryIndicator"
    - path: "pro_orc/lib/data/services/quick_actions_service.dart"
      provides: "openRemSleep method for claude CLI launch"
      contains: "openRemSleep"
  key_links:
    - from: "pro_orc/lib/features/shared/memory_indicator.dart"
      to: "MemoryData model"
      via: "accepts MemoryData? parameter"
      pattern: "MemoryData\\?"
    - from: "pro_orc/lib/features/code/code_project_card.dart"
      to: "pro_orc/lib/features/shared/memory_indicator.dart"
      via: "MemoryIndicator widget in title row"
      pattern: "MemoryIndicator"
    - from: "pro_orc/lib/features/code/code_project_card.dart"
      to: "pro_orc/lib/data/services/quick_actions_service.dart"
      via: "openRemSleep in quick actions list"
      pattern: "openRemSleep"
---

<objective>
Add memory status indicator and rem-sleep quick action to project cards.

Purpose: Users see at a glance whether each project has Claude memory consolidation and how fresh it is, and can trigger rem-sleep directly from the card.
Output: MemoryIndicator shared widget, updated Code/Research cards, openRemSleep service method.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pro_orc/lib/data/models/memory_data.dart
@pro_orc/lib/data/models/project_model.dart
@pro_orc/lib/features/code/code_project_card.dart
@pro_orc/lib/features/research/research_project_card.dart
@pro_orc/lib/data/services/quick_actions_service.dart
@pro_orc/lib/theme/n3_colors.dart
@pro_orc/lib/features/shared/status_badge.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MemoryIndicator widget and integrate into both card types</name>
  <files>
    pro_orc/lib/features/shared/memory_indicator.dart
    pro_orc/lib/features/code/code_project_card.dart
    pro_orc/lib/features/research/research_project_card.dart
  </files>
  <action>
Create `pro_orc/lib/features/shared/memory_indicator.dart` — a StatelessWidget `MemoryIndicator` that accepts `MemoryData? memory` and `AppColors colors` parameters.

Visual states (use `LucideIcons.bookMarked100` for the icon throughout — the "sleeping book" concept):
- **No memory** (`memory == null`): icon color = `colors.textDis` (disabled gray)
- **Memory exists, fresh** (`memory != null && !memory.isStale`): icon color = `colors.violet` (violet accent — memory/AI semantic)
- **Memory exists, stale** (`memory != null && memory.isStale`): icon color = `colors.amber` (amber = warning/attention)

Icon size: 13 (matches existing triangleAlert icon in card title rows).

Tooltip (German):
- No memory: `'Keine Memory vorhanden'`
- Memory exists: `'Letzte Konsolidierung: ${_formatDate(memory.lastConsolidated)}'` where `_formatDate` formats as `DD.MM.YYYY` using `DateFormat('dd.MM.yyyy')` from `intl` package. If `lastConsolidated` is null but `hasMemory` is true, show `'Memory vorhanden'`.

The widget returns a `Tooltip` wrapping an `Icon`.

**Integration into CodeProjectCard** (`code_project_card.dart`):
In the `_buildTitleRow` method, add the MemoryIndicator between the version text and the eye icon. Place it with a `const SizedBox(width: 4)` before it. Pass `widget.project.memory` and `colors`.

**Integration into ResearchProjectCard** (`research_project_card.dart`):
In the `_buildTitleRow` method, add the MemoryIndicator between the project name and the eye icon. Place it with a `const SizedBox(width: 4)` before it. Pass `widget.project.memory` and `colors`.

Use package imports only. Use `withValues(alpha:)` not `withOpacity()`. Import lucide_icons_flutter and the MemoryData model.

Note: The `intl` package is already a transitive dependency via Flutter — but check if it's in pubspec.yaml. If not, use manual string formatting: `'${d.day.toString().padLeft(2, '0')}.${d.month.toString().padLeft(2, '0')}.${d.year}'` to avoid adding a dependency.
  </action>
  <verify>
Run `cd /Users/rob/code/project_orchestration/pro_orc && flutter analyze` — no errors. Verify the MemoryIndicator widget exists and is imported in both card files.
  </verify>
  <done>
MemoryIndicator widget shows bookMarked icon in 3 visual states (gray/violet/amber), tooltip displays German consolidation date or "Keine Memory vorhanden", icon appears in title row of both CodeProjectCard and ResearchProjectCard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add openRemSleep quick action to QuickActionsService and both cards</name>
  <files>
    pro_orc/lib/data/services/quick_actions_service.dart
    pro_orc/lib/features/code/code_project_card.dart
    pro_orc/lib/features/research/research_project_card.dart
  </files>
  <action>
**QuickActionsService** (`quick_actions_service.dart`):
Add method `openRemSleep(String projectPath)` that opens Terminal.app and runs `claude` in the project directory. Use the same `Process.run('open', ['-a', 'Terminal', projectPath], runInShell: true)` pattern as `openInTerminal` — this opens a Terminal window at the project path where the user can then run rem-sleep commands. Add a doc comment explaining this opens Terminal for claude rem-sleep interaction.

**CodeProjectCard** (`code_project_card.dart`):
In `_buildQuickActions`, add a conditional quick action entry AFTER the existing actions (after Notion). Condition: `project.memory != null` (memory exists = rem-sleep is relevant). Use `LucideIcons.moonStar100` icon, tooltip `'Claude Memory'`, onPressed calls `qa.openRemSleep(project.path)`.

**ResearchProjectCard** (`research_project_card.dart`):
Same pattern — add the moonStar quick action conditionally when `project.memory != null`, after the existing actions.

This keeps the action simple and predictable — Terminal.app opens at the project directory, user runs `claude` to interact with memory.
  </action>
  <verify>
Run `cd /Users/rob/code/project_orchestration/pro_orc && flutter analyze` — no errors. Grep for `openRemSleep` in quick_actions_service.dart and both card files to confirm wiring.
  </verify>
  <done>
Quick action with moonStar icon appears on cards that have memory data, clicking opens Terminal.app at the project directory for claude rem-sleep interaction. QuickActionsService has openRemSleep method.
  </done>
</task>

</tasks>

<verification>
1. `flutter analyze` passes with no errors
2. `flutter build macos` succeeds
3. Both card files import and use MemoryIndicator
4. Both card files have moonStar quick action wired to openRemSleep
5. MemoryIndicator handles all 3 states: null memory, fresh memory, stale memory
</verification>

<success_criteria>
- Sleeping-book icon (bookMarked) visible on Code and Research cards showing memory status in 3 visual states
- Tooltip shows German date text or "Keine Memory vorhanden"
- MoonStar quick action appears on cards with memory, opens Terminal at project path
- flutter analyze passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-memory-ui-actions/13-01-SUMMARY.md`
</output>
