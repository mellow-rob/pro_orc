---
phase: 11-claude-tools-panel
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - pro_orc/lib/providers/claude_tools_watcher_provider.dart
  - pro_orc/lib/providers/claude_tools_provider.dart
  - pro_orc/lib/features/claude_tools/claude_tools_tab.dart
  - pro_orc/lib/features/claude_tools/skill_card.dart
  - pro_orc/lib/features/claude_tools/plugin_card.dart
  - pro_orc/lib/features/claude_tools/mcp_server_card.dart
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04]

must_haves:
  truths:
    - "Claude Tools tab shows three vertical sections: Skills, Plugins, MCP-Server — each with section header showing icon + name + count"
    - "Skills are displayed as mini GlassCards with amber accent, showing name, description, and action buttons (Finder, Homepage)"
    - "Plugins are displayed as mini GlassCards with emerald accent, showing name, version, enabled status, and Marketplace link button"
    - "MCP servers are displayed as mini GlassCards with violet accent, showing name, command/URL, and Config button"
    - "Search field at top filters all three sections live by name"
    - "Empty sections show per-section empty state text while remaining visible"
    - "Completely empty tab shows help text explaining what Skills, Plugins, and MCP servers are"
    - "File changes in ~/.claude/ trigger automatic re-scan and UI update"
  artifacts:
    - path: "pro_orc/lib/providers/claude_tools_watcher_provider.dart"
      provides: "StreamProvider watching ~/.claude/ for live updates"
      contains: "claudeToolsWatcherProvider"
    - path: "pro_orc/lib/providers/claude_tools_provider.dart"
      provides: "FutureProvider for ClaudeToolsData with watcher invalidation"
      contains: "claudeToolsProvider"
    - path: "pro_orc/lib/features/claude_tools/claude_tools_tab.dart"
      provides: "Full Claude Tools tab with search, three sections, empty states"
      contains: "class ClaudeToolsTab"
    - path: "pro_orc/lib/features/claude_tools/skill_card.dart"
      provides: "Mini GlassCard for Skills with amber accent"
      contains: "class SkillCard"
    - path: "pro_orc/lib/features/claude_tools/plugin_card.dart"
      provides: "Mini GlassCard for Plugins with emerald accent"
      contains: "class PluginCard"
    - path: "pro_orc/lib/features/claude_tools/mcp_server_card.dart"
      provides: "Mini GlassCard for MCP servers with violet accent"
      contains: "class McpServerCard"
  key_links:
    - from: "pro_orc/lib/providers/claude_tools_provider.dart"
      to: "pro_orc/lib/data/services/claude_tools_scanner.dart"
      via: "ClaudeToolsScanner().scanAll() call"
      pattern: "ClaudeToolsScanner.*scanAll"
    - from: "pro_orc/lib/providers/claude_tools_provider.dart"
      to: "pro_orc/lib/providers/claude_tools_watcher_provider.dart"
      via: "ref.listen for invalidation"
      pattern: "ref\\.listen.*claudeToolsWatcherProvider"
    - from: "pro_orc/lib/features/claude_tools/claude_tools_tab.dart"
      to: "pro_orc/lib/providers/claude_tools_provider.dart"
      via: "ref.watch(claudeToolsProvider)"
      pattern: "ref\\.watch.*claudeToolsProvider"
---

<objective>
Wire up the Riverpod providers and build the complete Claude Tools tab UI with search, three sections, mini cards, and live updates.

Purpose: This plan delivers the full user-visible Claude Tools feature — the reactive provider chain (watcher → provider → UI) and all presentation widgets (tab, search, section headers, three card types, empty states).

Output: 2 provider files, 1 tab widget, 3 card widgets — replacing the existing stub tab.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-claude-tools-panel/11-RESEARCH.md
@.planning/phases/11-claude-tools-panel/11-01-SUMMARY.md
@pro_orc/lib/providers/watcher_provider.dart
@pro_orc/lib/providers/projects_provider.dart
@pro_orc/lib/features/claude_tools/claude_tools_tab.dart
@pro_orc/lib/features/shell/glass_card.dart
@pro_orc/lib/theme/n3_colors.dart
@pro_orc/lib/data/services/watcher_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Riverpod providers for Claude Tools</name>
  <files>
    pro_orc/lib/providers/claude_tools_watcher_provider.dart
    pro_orc/lib/providers/claude_tools_provider.dart
  </files>
  <action>
    **claude_tools_watcher_provider.dart** — StreamProvider watching `~/.claude/`:
    - `final claudeToolsWatcherProvider = StreamProvider<WatchEvent>((ref) async* { ... })`
    - `ref.keepAlive()` — never disposed (same pattern as watcherProvider)
    - Resolve home dir: `Platform.environment['HOME'] ?? '/Users/rob'`
    - Create `WatcherService('$home/.claude')` (single dir, not multi)
    - `ref.onDispose(service.dispose)`
    - `yield* service.events` — forward debounced events
    - Import `dart:io` for Platform, `watcher` for WatchEvent, `flutter_riverpod`, and the app's WatcherService
    - Use package imports only

    **claude_tools_provider.dart** — FutureProvider with ref.listen invalidation:
    - `final claudeToolsProvider = FutureProvider<ClaudeToolsData>((ref) async { ... })`
    - `ref.listen(claudeToolsWatcherProvider, (previous, next) { if (next.hasValue) ref.invalidateSelf(); })`
    - `return ClaudeToolsScanner().scanAll()`
    - Import ClaudeToolsScanner, ClaudeToolsData, and the watcher provider
    - Use package imports only
    - Follows exact same pattern as projectsProvider → no deviation
  </action>
  <verify>
    Run `flutter analyze` from `pro_orc/` — no errors in new provider files.
  </verify>
  <done>
    claudeToolsWatcherProvider watches `~/.claude/` and claudeToolsProvider re-scans on any file change event. Provider chain mirrors the proven watcher→projects pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Claude Tools tab + card widgets</name>
  <files>
    pro_orc/lib/features/claude_tools/claude_tools_tab.dart
    pro_orc/lib/features/claude_tools/skill_card.dart
    pro_orc/lib/features/claude_tools/plugin_card.dart
    pro_orc/lib/features/claude_tools/mcp_server_card.dart
  </files>
  <action>
    **claude_tools_tab.dart** — Replace the existing stub entirely. Make it a `ConsumerStatefulWidget` (needs TextEditingController for search):

    State fields:
    - `TextEditingController _searchController`
    - `String _searchQuery = ''` (updated via listener on controller)

    Build method:
    - `ref.watch(claudeToolsProvider)` — handle AsyncValue states (loading, error, data)
    - Loading: show centered CircularProgressIndicator with cyan accent
    - Error: show centered error text with textSec color
    - Data: check `data.isEmpty` for full empty state, otherwise build scrollable content

    **Full empty state** (all three sections empty):
    - Centered GlassCard with help text explaining:
      - "Keine Claude Tools gefunden"
      - Brief explanation of what Skills, Plugins, and MCP-Server are
      - How to install them (e.g., "Skills: Erstelle einen Ordner in ~/.claude/skills/")
    - Use textSec color for body text, textPri for heading

    **Content layout** (SingleChildScrollView > Column):
    - Search TextField at top: outlined style, "Suchen..." placeholder, prefixIcon search icon, clear button when non-empty. Styled with bgElev fill, textPri text, cyanLo border focus color.
    - SizedBox(height: 24) spacing
    - Three sections in order: **Skills → Plugins → MCP-Server** (per research recommendation)

    **Each section** (`_buildSection` helper method):
    - Section header row: Icon + Text + count badge. Example: "🔧 Skills (7)" — use actual Unicode emoji or Icons equivalent. Text in textPri, count in textSec.
      - Skills icon: Icons.handyman or ⚡ equivalent
      - Plugins icon: Icons.extension
      - MCP-Server icon: Icons.dns
    - SizedBox(height: 12) after header
    - If section has items (after search filter): `Wrap` widget with `spacing: 12, runSpacing: 12` containing cards
    - If section is empty after filter: subtle empty state text ("Keine Skills installiert" / "Keine Plugins installiert" / "Keine MCP-Server konfiguriert") in textDim color
    - SizedBox(height: 32) between sections

    **Search filtering:**
    - Filter each list by `_searchQuery` (case-insensitive match on name field only — locked decision)
    - If search query is empty, show all items
    - All three sections always visible (even if filtered to zero — per locked decision)

    **skill_card.dart** — Mini GlassCard with amber accent:
    - `StatelessWidget` taking `SkillData skill`
    - GlassCard wrapper, compact padding (12px)
    - Width: constrained via SizedBox(width: 240)
    - Content Column:
      - Name: bold, `colors.amber` color, maxLines 1, overflow ellipsis
      - Description: `colors.textSec`, maxLines 2, overflow ellipsis. If null, show "Kein Beschreibung" in textDim italic.
      - SizedBox(height: 8)
      - Action row (Row with small IconButtons):
        - Finder icon (Icons.folder_open): calls `Process.run('open', [skill.path], runInShell: true)` to open skill dir in Finder
        - Homepage icon (Icons.open_in_browser): if `skill.homepage != null`, call `launchUrl(Uri.parse(skill.homepage!))`. Hide button if no homepage.
      - Action icons in amberLo color, hover/splash in amber with low alpha

    **plugin_card.dart** — Mini GlassCard with emerald accent:
    - `StatelessWidget` taking `PluginData plugin`
    - GlassCard wrapper, compact padding (12px), SizedBox(width: 240)
    - Content Column:
      - Name: bold, `colors.emerald` color, maxLines 1, overflow ellipsis
      - Row: version badge (if version != null, small text in textDim) + enabled/disabled indicator (small dot or text "Aktiv"/"Inaktiv" in emeraldLo/textDim)
      - Description: `colors.textSec`, maxLines 2, overflow ellipsis. If null, omit.
      - SizedBox(height: 8)
      - Action row:
        - Marketplace icon (Icons.store): if `plugin.marketplaceUrl != null`, call `launchUrl(Uri.parse(plugin.marketplaceUrl!))`. Hide if no URL.
      - Action icons in emeraldLo color

    **mcp_server_card.dart** — Mini GlassCard with violet accent:
    - `StatelessWidget` taking `McpServerData server`
    - GlassCard wrapper, compact padding (12px), SizedBox(width: 240)
    - Content Column:
      - Name: bold, `colors.violet` color, maxLines 1, overflow ellipsis
      - Type badge: small text showing server.type.name (stdio/http/sse) in violetLo
      - Command/URL: `colors.textSec`, maxLines 2, overflow ellipsis, monospace font (use `fontFamily: 'JetBrains Mono'` or system monospace)
      - SizedBox(height: 8)
      - Action row:
        - Config icon (Icons.settings): calls `Process.run('open', ['$claudeDir/settings.json'], runInShell: true)` where claudeDir comes from `Platform.environment['HOME']`/.claude. Opens settings.json in default editor.
      - Action icons in violetLo color

    All cards use package imports only. Import `dart:io` for Process.run, `url_launcher` for launchUrl. Follow existing CodeProjectCard patterns for hover, layout, and icon styling.
  </action>
  <verify>
    Run `flutter analyze` from `pro_orc/` — zero errors across all new/modified files.
    Run `flutter test` — all existing tests pass.
    Run `flutter run -d macos` — Claude Tools tab loads, shows discovered skills and plugins (MCP section will be empty per current machine state).
  </verify>
  <done>
    Claude Tools tab displays three sections (Skills, Plugins, MCP-Server) with mini cards. Search field filters by name. Each card shows relevant metadata and action buttons. Empty states display correctly. File watcher triggers live updates.
  </done>
</task>

</tasks>

<verification>
- `flutter analyze` passes with zero errors
- `flutter test` passes — no regressions
- Claude Tools tab renders three sections with section headers and counts
- Skill cards show name + description from SKILL.md frontmatter
- Plugin cards show name + version + enabled status
- MCP server section shows empty state (currently no servers configured)
- Search field filters cards by name across all sections
- Adding a file to `~/.claude/skills/` triggers live update in the tab
</verification>

<success_criteria>
- Claude Tools tab replaces the stub with full three-section layout
- Each tool type has its own mini card with type-specific accent color and actions
- Provider chain (watcher → provider → UI) enables live updates from `~/.claude/` changes
- All locked decisions from CONTEXT.md are implemented: sections (not tabs), mini cards, search by name only, per-section empty states, section headers with icon + count
</success_criteria>

<output>
After completion, create `.planning/phases/11-claude-tools-panel/11-02-SUMMARY.md`
</output>
