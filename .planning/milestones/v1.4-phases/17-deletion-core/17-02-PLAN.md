---
phase: 17-deletion-core
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - pro_orc/lib/features/shared/delete_project_dialog.dart
  - pro_orc/lib/features/code/code_project_card.dart
  - pro_orc/lib/features/research/research_project_card.dart
autonomous: true
requirements:
  - DEL-03
  - DEL-05
must_haves:
  truths:
    - "User sees a confirmation dialog after clicking 'Projekt loeschen'"
    - "Delete button is disabled until user types the exact project name"
    - "Delete button becomes enabled only when typed name matches exactly"
    - "After confirmed deletion the project disappears from the dashboard without manual reload"
  artifacts:
    - path: "pro_orc/lib/features/shared/delete_project_dialog.dart"
      provides: "GitHub-style confirmation dialog with name input and delete action"
      contains: "DeleteProjectDialog"
    - path: "pro_orc/lib/features/code/code_project_card.dart"
      provides: "Wired _confirmDelete calling DeleteProjectDialog"
      contains: "DeleteProjectDialog"
    - path: "pro_orc/lib/features/research/research_project_card.dart"
      provides: "Wired _confirmDelete calling DeleteProjectDialog"
      contains: "DeleteProjectDialog"
  key_links:
    - from: "pro_orc/lib/features/shared/delete_project_dialog.dart"
      to: "pro_orc/lib/data/services/deletion_service.dart"
      via: "calls deleteProject on confirm"
      pattern: "deleteProject"
    - from: "pro_orc/lib/features/shared/delete_project_dialog.dart"
      to: "pro_orc/lib/providers/projects_provider.dart"
      via: "ref.invalidate(projectsProvider) after deletion"
      pattern: "invalidate.*projectsProvider"
    - from: "pro_orc/lib/features/code/code_project_card.dart"
      to: "pro_orc/lib/features/shared/delete_project_dialog.dart"
      via: "_confirmDelete shows dialog"
      pattern: "DeleteProjectDialog"
---

<objective>
Build the GitHub-style confirmation dialog and wire it into both card types, with provider invalidation for auto-refresh.

Purpose: Prevent accidental deletion by requiring exact project name input, and ensure the dashboard updates automatically after deletion.
Output: delete_project_dialog.dart, updated _confirmDelete in both card types.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-deletion-core/17-01-SUMMARY.md
@pro_orc/lib/features/shared/create_project_dialog.dart
@pro_orc/lib/features/code/code_project_card.dart
@pro_orc/lib/features/research/research_project_card.dart
@pro_orc/lib/data/services/deletion_service.dart
@pro_orc/lib/providers/projects_provider.dart
@pro_orc/lib/theme/n3_colors.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeleteProjectDialog with GitHub-style name confirmation</name>
  <files>pro_orc/lib/features/shared/delete_project_dialog.dart</files>
  <action>
Create `delete_project_dialog.dart` as a `ConsumerStatefulWidget`. Follow the dialog pattern from `create_project_dialog.dart` for styling (n3 theme, glassmorphism dark styling).

The dialog must:
1. Accept `ProjectModel project` as constructor param
2. Show project name prominently (bold, textPri color)
3. Show a warning text: "Diese Aktion kann nicht rueckgaengig gemacht werden. Der Ordner wird permanent geloescht."
4. Show a `TextField` with hint text: "Projektname zur Bestaetigung eingeben"
5. Show a "Loeschen" button that is DISABLED (greyed out) by default
6. Enable the "Loeschen" button ONLY when `_textController.text == project.displayName` (exact match, case-sensitive)
7. Use a `TextEditingController` with `addListener` to trigger `setState` on every keystroke for real-time button enable/disable
8. On confirm: call `deleteProject(project.path)` from deletion_service.dart, then `ref.invalidate(projectsProvider)` for auto-refresh, then `Navigator.of(context).pop(true)`
9. Show a "Abbrechen" cancel button that pops with `false`
10. "Loeschen" button should use red/error coloring (use `Colors.red.shade700` or similar destructive color) when enabled, and `colors.textDim` when disabled

Dialog layout (use `showDialog` with `AlertDialog` or custom Dialog):
- Title: "Projekt loeschen"
- Content: warning text + name display + text field
- Actions: Abbrechen (left), Loeschen (right, conditionally enabled)

Use `ref.invalidate(projectsProvider)` after successful deletion — this triggers the existing watcher-driven rescan, removing the deleted project from the list automatically. This is the same pattern used in `_ignoreProject()` and `_setProjectType()` on both cards.

Style the dialog background with `colors.bgElev` and text with `colors.textPri`/`colors.textSec` per n3 theme convention. Add `BackdropFilter` blur if desired (matching create_project_dialog pattern).
  </action>
  <verify>Run `flutter analyze` from pro_orc/ — no errors.</verify>
  <done>DeleteProjectDialog exists with: name display, warning text, text input, disabled-by-default Loeschen button that enables only on exact name match, calls deleteProject + invalidates projectsProvider on confirm.</done>
</task>

<task type="auto">
  <name>Task 2: Wire _confirmDelete in both card types to show DeleteProjectDialog</name>
  <files>
    pro_orc/lib/features/code/code_project_card.dart
    pro_orc/lib/features/research/research_project_card.dart
  </files>
  <action>
In both `code_project_card.dart` and `research_project_card.dart`, replace the stub `_confirmDelete` method (created in 17-01) with the actual implementation:

```dart
void _confirmDelete(BuildContext context) {
  showDialog<bool>(
    context: context,
    builder: (_) => DeleteProjectDialog(project: widget.project),
  );
}
```

Add the import at the top of both files:
```dart
import 'package:pro_orc/features/shared/delete_project_dialog.dart';
```

The dialog itself handles the deletion and provider invalidation — the card just needs to show it. No return value handling needed because the dialog invalidates projectsProvider directly, which triggers the reactive rescan.
  </action>
  <verify>
Run `flutter analyze` from pro_orc/ — no errors. Manually test:
1. Right-click a Code card -> "Projekt loeschen" -> dialog appears
2. Type wrong name -> "Loeschen" button stays disabled
3. Type exact project name -> "Loeschen" button enables
4. Right-click a Research card -> same behavior
5. Click "Abbrechen" -> dialog closes, nothing happens
  </verify>
  <done>Both Code and Research cards show DeleteProjectDialog when "Projekt loeschen" is selected from context menu. Button enables only on exact name match. Successful deletion auto-refreshes the dashboard.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` passes with no new errors
2. Right-click Code card -> "Projekt loeschen" -> dialog with name input appears
3. Right-click Research card -> "Projekt loeschen" -> dialog with name input appears
4. "Loeschen" button disabled until exact project name typed
5. After deletion, project disappears from dashboard without manual reload
6. "Abbrechen" closes dialog without side effects
</verification>

<success_criteria>
- DEL-03: User must type exact project name to enable delete button (GitHub-style confirmation)
- DEL-05: Dashboard auto-refreshes after deletion via ref.invalidate(projectsProvider)
</success_criteria>

<output>
After completion, create `.planning/phases/17-deletion-core/17-02-SUMMARY.md`
</output>
