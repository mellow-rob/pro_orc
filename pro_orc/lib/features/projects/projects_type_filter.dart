/// Active type filter for the Projekte tab's header chips (FR-021).
/// `all` shows every project regardless of [pro_orc.data.models.project_type.ProjectType].
enum ProjectsTypeFilter { all, code, research }
